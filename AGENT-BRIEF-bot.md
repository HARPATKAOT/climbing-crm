# תדריך סוכן — הבוט של קיר בועז

מסמך מצב, לא היסטוריה. מי שממשיך לעבוד על הבוט מתחיל כאן, ואז קורא
`PROJECT-CONTEXT.md` (הקשר המערכת) ו-`docs/BOT-STATUS.md` (יומן ההתפתחות).

עודכן: 5.8.2026 · 674 בדיקות עוברות ב-`npm test`

---

## מה הבוט עושה

עונה ללקוחות בוואטסאפ בשם העסק. **מצב כלים**: המודל (Gemini) קורא את ההודעה
ומבקש מהמערכת את העובדה שהוא צריך, במקום ששכבת מילות מפתח תנחש כוונה. הוא גם
מבצע פעולות — משבץ לקבוצה, רושם מתעניינים, קובע לעצמו תזכורות, מעדכן סטטוס.

**כלל העל:** כל עובדה — מחיר, שעה, מקום פנוי, קישור — מגיעה מכלי בלבד. המודל
מנסח, לא ממציא. יש אכיפה בקוד, לא רק בפרומפט (ראו „הגנות” למטה).

---

## ארכיטקטורה — סדר הטיפול בהודעה נכנסת

`server/whatsapp.js` → `handleIncomingMessage`. הסדר קובע, וכל שלב חוסם את הבא:

1. **נעילת כפילות** — מטא שולחת אותו אירוע פעמיים; נתפס לפי `meta_message_id`
2. **יצירת/איתור כרטיס** — `db.createLeadFromWhatsApp`. הודעה ממספר של מתאמן
   מתויקת לכרטיס ההורה (`matchedVia === 'child_phone'`)
3. **שמירה דורבנית** — אם ההודעה לא נשמרה, הלקוח לא נכנס לתור ואין מענה
4. **סוכן צוות** — מספר ב-`aiStaffPhones` מקבל את סוכן ה-CRM, לא את בוט הלקוחות
5. **מדיה** — תמונה/הקלטה/קובץ: מענה מיידי + התראת צוות. המודל לא רואה מדיה
6. **דיווח למתנ״ס** — מספר ב-`aiCentrePhones`: ענף קוד נפרד (ראו למטה)
7. **שער ההחלטה** (`decideBotGate`) — מתג ראשי, opt-out, השהיה, שיחת צוות,
   שעות פעילות, קהל, מגבלת קצב, בקשת נציג מפורשת
8. **מסלול הכלים** (`botToolTurn.js`) — עד 7 צעדי מודל
9. **גיבוי** — אם המודל נכשל, המסלול הישן (`buildHeuristicReply`) עונה

כיבוי `aiToolsEnabled` מחזיר למסלול הישן מיידית. זו רשת הביטחון.

---

## הכלים (17)

`server/botTools.js` — ההצהרות והמימוש באותו קובץ.

| קריאה | מה מחזיר |
|---|---|
| `listClasses` | קבוצות לפי כיתה/שכבה/יום, מקומות, מדריך, רמה, תדירויות |
| `getPrices` | מחירי חוגים, ציוד, דמי העשרה |
| `getEquipmentInfo` | למה צריך כל פריט — בלשון העסק |
| `getOpeningHours` | שעות מהיומן |
| `getEvents` | אירועים לפרסום + „מזהה” לכל אחד |
| `getSignupLink` | קישור הרשמה של קבוצה אחת |
| `getHealthDeclarations` | מי חתם על טופס ההשתתפות, עד מתי |
| `getEquipmentPaymentLink` | ציוד שלא שולם + קישור. **בלי סכום** |
| `getRegistrationPack` | שלושת שלבי ההרשמה בסדר |
| `getFamilyCard` | הילדים, הקבוצה והסטטוס שלהם |

**כלים שכותבים:** `startSignup` · `cancelSignup` · `joinWaitlist` ·
`addActivityInterest` · `scheduleFollowUp` · `updateCustomerDetails`

`updateCustomerDetails` מקבל **רק** שם פרטי ושם משפחה, ושניהם חובה. תעודת
זהות, תאריך לידה וכל יתר פרטי ההרשמה נאספים בטופס ולא בשיחת וואטסאפ.

---

## הגנות — אלה לא הצעות, אלה קוד

מי שמשנה כאן צריך להבין למה זה קיים. כל אחת נכתבה אחרי תקלה אמיתית.

**שיבוץ נעצר ברשום.** `isRegisteredTrainee` — מתאמן בסטטוס `registered`/`active`
לא יזוז. כל שלב מוקדם יותר במסע (ליד, חתם, ממתין, נקבע היכרות, שילם היכרות,
היה רשום, המתנה) פתוח לבוט. יש בדיקה שרצה על כל הסטטוסים בקטלוג.

**קישור שהמודל המציא לא נשלח.** `unknownUrlsInReply` — תשובה יכולה להכיל רק
כתובות שכלי החזיר בתור הזה או שהפרומפט סיפק. אחרת: העברה לצוות. נכתב אחרי
שהמודל לקח קישור של קבוצה אחת והחליף בו את שם הקבוצה.

**יכולת כבויה לא נמסרת למודל.** `enabledToolNames` מסננת את ההצהרות עצמן, לא
רק דוחה קריאה. המודל לא יכול לשכנע את עצמו להשתמש בכלי שהוא לא רואה.

**נבחרת לא מוצעת מיוזמת הבוט.** רק אם ביקשו `level: 'נבחרת'` במפורש. שיבוץ
מפורש לנבחרת כן עובד.

**תדירות שלא קיימת לא מוצעת.** מחיר חסר אינו מחיר 0. `תדירויות_אפשריות` מכילה
רק את מה שבאמת נמכר.

**היסטוריה מתויגת בזמן.** הודעה מעל 3 שעות מסומנת `[לפני X שעות]`, כי למודל
אין שעון והוא ענה על תמונה מלפני שלוש שעות.

---

## פאנל היכולות

**דיוור ← הגדרות ואימון AI ← הגדרות הבוט**, בתוך כרטיס המתג הראשי.
12 מתגים, נשמרים מיד, נאכפים בשרת. `server/botCapabilities.js`.

```
classes · hours · events · event_interest(תלוי ב-events) · health ·
signup_links · placement · equipment · follow_ups · save_name ·
family_card · centre_report
```

ברירת המחדל של כל יכולת דלוקה, כדי שהוספת מתג לא תשנה התנהגות בשקט.
`centre_report` הוא היחיד שאין לו כלי — הוא חוסם ענף בקוד — ויש לו שדה טלפון.

**בדיקה שומרת:** כל כלי חייב להשתייך למתג אחד בדיוק. כלי בלי בעלים הוא כלי
שאי אפשר לכבות, והבדיקה תיפול. זה כבר תפס כלי חדש שנוסף בלי מתג.

---

## מעקבים — התזמון הוא העיקר

`server/botFollowUps.js` · אוסף `bot_followups` · ריצה כל 15 דקות.

מטא מרשה טקסט חופשי רק 24 שעות מההודעה האחרונה של הלקוח, ומעקב הוא בהגדרה
יום אחרי — כלומר אחרי שהחלון נסגר. לכן:

- **מעקב קצר** נקבע ל-**23 שעות** אחרי ההודעה האחרונה. בתוך החלון, בלי תבנית
- אם 23 שעות נופלות בשעה לא אנושית (הלקוח כתב ב-4 לפנות בוקר) — ההודעה
  מוקדמת לרגע האחרון שהוא גם בחלון וגם בשעות הפעילות
- **מעקב ארוך** („נדבר בספטמבר”) דורש תבנית `bot_followup_v1`

התבנית `bot_followup_v1` מאושרת ב-Meta (`APPROVED`, אומת ב-4.8.2026), ולכן
מעקב ארוך יכול להישלח אוטומטית גם לאחר סגירת חלון 24 השעות.

כל שיבוץ „ממתין להרשמה” קובע אוטומטית בדיקה ליום שאחרי — בקוד השיבוץ, לא
בזיכרון של המודל, אחרת אי אפשר לדעת מי מכוסה.

---

## דיווח למתנ״ס (כרמית)

`server/centreReport.js` · מופעל רק כשיש מספר ב-`aiCentrePhones`.

כרמית כותבת שם של ילד. הבוט מוצא את **האימון הראשון לחיוב** — ההגעה המוקדמת
ביותר שאיננה אימון היכרות — עונה לה עם התאריך, ומסמן את הילד „רשום”.

אין ניחוש בקיזוז ההיכרות: שורת נוכחות של היכרות נושאת סטטוס משלה
(`intro_attended`) שנקבע כשהיא נוצרה. שם עמום, שם שלא נמצא, או ילד בלי
נוכחות — עוברים לצוות עם הסיבה.

**תלוי בנוכחות מסומנת.** כרגע יש 3 שורות נוכחות בכל המסד — בפועל התהליך
יעביר הכול לצוות עד שהמדריכים יסמנו.

---

## יומן הפעולות

`server/botActivityLog.js` · אוסף `bot_actions` · מסך: טאב „יומן פעולות”.

„פעולות” = מה שהבוט שינה. „הודעות” = מה שהוא אמר. זה המקום היחיד שעונה על
„מה הבוט עשה היום”, וממנו תופסים טעות שיטתית לפני שהיא חוזרת מאה פעם.

---

## מלכודות — לקרוא לפני שנוגעים

1. **פריסה באמצע שיחה בולעת הודעה.** השרת עולה מחדש, ההודעה נשמרת, התשובה לא
   יוצאת. לא לבדוק בוואטסאפ בזמן פריסה.
2. **סקריפט חייב לרוץ מתוך `server/`** — אחרת `.env` לא נטען, אין מפתח מודל,
   והבוט עונה מהמסלול הישן בלי שום סימן.
3. **`db.set` דורס את המטמון המקומי.** סקריפט שכותב אוסף שלם מוחק את מה
   שהשרת המקומי הספיק לכתוב.
4. **השרת החי טוען את הנתונים בעלייה בלבד.** שינוי ישיר במסד לא מגיע לבוט
   עד פריסה או הפעלה מחדש.
5. **הגדרות שמורות גוברות על ברירות המחדל בקוד.** שינוי `DEFAULT_BOT_SETTINGS`
   לא משנה כלום בשרת החי — הערך יושב ב-`app_settings.whatsapp_settings`.
6. **מגרש הבדיקות: `0599111000`.** שיחה משם לא יוצאת ללקוח אמיתי, אבל כן
   משנה נתונים אמיתיים של אותו כרטיס.
7. **יש סשן נוסף שעובד באותה תיקייה.** לוודא מה נכנס ל-commit לפני שדוחפים.

---

## איך בודקים

```bash
cd server && npm test
```

שיחה אמיתית מול המודל בלי לגעת בלקוחות (מתוך `server/`):

```js
process.env.BOT_TOOLS_ENABLED = 'true';
const { whatsappService } = await import('./whatsapp.js');
await whatsappService.handleIncomingMessage('0599111000', 'היי', true);
```

הארגומנט האחרון `true` = סימולטור: לא יוצאת הודעה, ולא נשלחות התראות צוות.

---

## מה פתוח

1. **נוכחות כמעט לא מסומנת** — חוסם את הדיווח למתנ״ס בפועל
2. **`aiCentrePhones` ריק** — התהליך מול כרמית כבוי עד שיוזן מספר
3. **הבוט לא קובע אימון היכרות** — `intro_scheduled` נשאר ידני
4. **קישורים לדומיין ה-API.** חלק מהקישורים הציבוריים יוצאים דרך
   `climbing-crm-api.onrender.com` ולא `app.kirboaz.co.il` — תלוי
   ב-`FRONTEND_URL` / `PUBLIC_APP_URL` ב-Render

## נסגר לאחרונה (5.8.2026)

- איסוף שם כבר אינו תלוי במנוע התשובה: לקוח חדש או כרטיס עם שם פרטי בלבד
  נעצר עד שנשמרים שם פרטי ושם משפחה. השאלה המקורית נשמרת ונענית מיד לאחר מכן.
- כלי עדכון הפרטים צומצם לשם פרטי ומשפחה בלבד; עדכון תאריך לידה הוסר מכלי
  הבוט ועובר דרך טופס ההרשמה.

- תבנית המעקב הארוך `bot_followup_v1` מאושרת ב-Meta
- מחיר כניסה בודדת — מוצר «כניסה לקיר» בקטגוריית «כניסה» במחירון; `getPrices` מחזיר אותו
- ריאקציה בוואטסאפ נשמרת בשיחה אבל לא מפעילה את הבוט — גם מחוץ לשעות הפעילות
- מדיה ללא כיתוב מזוהה גם כשה-placeholder בעברית, ומטופלת לפני הודעת מחוץ לשעות
- „הסרת אחריות” אינה מפעילה יותר opt-out; מילות עצירה דורשות מילה/ביטוי שלמים
- ההודעה הנוכחית אינה מוכפלת בהיסטוריה שנשלחת למודל, והיסטוריה 0 באמת מכבה היסטוריה
- שדה הטלפון של המתנ״ס נשמר גם בלי שינוי מתג באותה בקשה
- `aiLeadCaptureEnabled` מוצג רק במסלול הגיבוי הישן שבו הוא באמת משפיע
- `npm test` כולל עכשיו גם יכולות, כלים, מעקבים, למידה, יומן ודיווח למתנ״ס
- קבוצה ששמה כולל שני ימי אימון (למשל `ב׳+ה׳`) מוחזרת למודל עם שניהם; סינון לפי אחד מהם מזהה אותה בכל מסלולי הבוט

---

## קבצים

| קובץ | תפקיד |
|---|---|
| `server/whatsapp.js` | כניסת הודעות, שער, שליחה, סימון 🧗 |
| `server/whatsappBot.js` | כללים, גבולות, השהיות, היסטוריה, זיהוי |
| `server/botTools.js` | הכלים — הצהרה ומימוש |
| `server/botToolTurn.js` | הרצת התור מול המודל + הגנת קישורים |
| `server/botCapabilities.js` | המתגים והאכיפה |
| `server/botFollowUps.js` | תזמון מעקבים |
| `server/botActivityLog.js` | יומן פעולות |
| `server/centreReport.js` | דיווח למתנ״ס |
| `server/participationForm.js` | שם הטופס — במקום אחד |
| `client/src/components/BotCapabilitiesPanel.jsx` | פאנל המתגים |
| `client/src/components/BotActivityPanel.jsx` | מסך היומן |
