# פרומפט לסוכן: בניית מודול "מרכז פיננסי" למערכת ניהול קיר הטיפוס

> **איך להשתמש במסמך הזה:** שמור אותו בריפו כ־`FINANCE_SPEC.md` ותן לסוכן הוראה אחת: *"קרא את `FINANCE_SPEC.md` ובצע אותו מקצה לקצה לפי פרוטוקול ההרצה בסעיף 1.5."* לפני שאתה שולח — **מלא את כל ה־`<<< ... >>>` בסעיף 0**. בהרצה במכה אחת הסוכן לא יעצור לשאול אותך; כל שדה ריק יהפוך להנחה שהוא ימציא.

---

## 1.5 פרוטוקול הרצה — ריצה אוטונומית במכה אחת

אתה מבצע את כל המסמך הזה בסשן אחד מתמשך, בלי לחכות לאישור ביניים. כדי שזה יעבוד:

**א. לפני שורת קוד ראשונה**
1. קרא את כל הריפו הרלוונטי ומפה: טבלאות קיימות, מסכים קיימים, קוד ה־iCount הקיים, שכבת ה־auth וה־jobs.
2. צור `PROGRESS.md` בשורש הריפו עם רשימת כל השלבים (0 → 7), כל אחד עם סטטוס `todo/doing/done/blocked`, קריטריוני הקבלה שלו, והחלטות שקיבלת.
3. צור `DECISIONS.md` — כל הנחה שלקחת בגלל מידע חסר, בשורה אחת עם נימוק. זה המסמך שאני קורא בסוף.
4. אם משהו **חוסם באמת** (אין גישה ל־API, סכמה שסותרת את המפרט, אין credentials) — אל תעצור את כל הריצה. סמן `blocked`, בנה מאחורי interface עם mock, המשך הלאה, ורשום את זה בראש `PROGRESS.md`.

**ב. במהלך הריצה**
5. עבוד **שלב אחרי שלב לפי סעיף 10**, לא במקביל. אל תתחיל שלב לפני שהקודם עובר את קריטריון הקבלה שלו.
6. **שער בדיקה בסוף כל שלב** (חובה, לא אופציה): הקוד עולה, המיגרציות רצות קדימה ואחורה, הטסטים עוברים, והמסך הרלוונטי נטען בלי שגיאות קונסול. אם השער נכשל — תקן לפני שממשיכים. אל תצבור חוב לשלב הבא.
7. **commit נפרד לכל שלב** עם הודעה `feat(finance): phase N — ...`, ועדכון `PROGRESS.md` באותו commit. אם הסשן ייפול, אני צריך לדעת בדיוק איפה עצרת.
8. אם נגמר לך הקונטקסט — סכם ב־`PROGRESS.md` את המצב המדויק ואת הצעד הבא, כך שסשן חדש ימשיך מהמסמך בלי לשאול אותי כלום.
9. **בנה vertical slices.** עדיף פיצ׳ר אחד שעובד מהסכמה ועד המסך, מאשר שכבה שלמה שלא מחוברת לכלום. אל תשקיע בליטוש של שלב מוקדם על חשבון שלב מאוחר.
10. אל תעשה gold plating. אין refactor רחב, אין החלפת ספריות, אין שינוי ארכיטקטורה כללית של הפרויקט. אתה מוסיף מודול, לא כותב מחדש מערכת.

**ג. עצירות חובה — כאן כן תעצור ותשאל אותי**
- שינוי סכמה **שובר** בטבלאות קיימות (לקוחות, הרשמות, קופה, עובדים)
- כל פעולה שנוגעת בכסף אמיתי: הנפקת מסמך אמיתי ב־iCount, חיוב לקוח, שליחת מייל ללקוח או לספק
- הזנת credentials אמיתיים של בנק/אשראי/מייל
- מחיקת נתונים כלשהי

בכל אלה: בנה את הקוד, השאר אותו **מאחורי feature flag כבוי**, ורשום ב־`PROGRESS.md` "ממתין לאישור ידני".

**ד. בסוף הריצה**
דוח סיכום אחד: מה נבנה, מה נשאר `blocked` ולמה, אילו הנחות לקחת, איך לבדוק ידנית כל שלב, ומה נדרש ממני (מפתחות, אישורים, החלטות עסקיות).

---

## 0. הקשר שאתה חייב למלא לפני השליחה

```
# --- מולא מדוח הסריקה של הסוכן (Recon), 2026-08-15 ---
STACK:            Node.js 20 בפרודקשן (Render), 24 מקומי. Express 4.19, ESM.
                  אין ORM — שכבת db.js (קאש קריאה בזיכרון + db.json מקומי) מעל
                  Supabase (Postgres, פרנקפורט) דרך supa.js. קליינט: React 18 +
                  Vite 8 + react-router-dom 7 + lucide-react, פריסה ב-Vercel על
                  push ל-main. אין ספריית גרפים — גרפים קיימים הם SVG/CSS ידני.
                  Jobs: שניים — Render Cron שקורא ל-endpoints עם requireCronSecret
                  (סנכרון iCount רץ כך כל 15 דקות), ולולאות setInterval בתוך
                  התהליך, פועלות רק בפרודקשן (scheduledJobsEnabled).
REPO:             מונו-ריפו: server/ (API), client/ (React SPA). דף "דוחות
                  פיננסיים" קיים: client/src — מסך Finance (טאבים: סקירה,
                  תשלומים ועסקאות, הכנסות, הוצאות וספקים, קליטה והתאמה,
                  רווחיות ותרות, התאמות ואיכות נתונים) מול server/financeRoutes.js.
DB:               Supabase Postgres. שני סוגי טבלאות: DIRECT_TABLES (עמודות SQL
                  אמיתיות + mappers ב-supa.js) ו-OPERATIONAL_TABLES (אוסף kv —
                  JSON durable, הוספת טבלה/שדה בלי מיגרציית SQL). קיימות:
                  parents 1615, students 1256, groups 17, enrollments 67,
                  activities 84, activity_registrations 405, payments 117,
                  pos_sales 45, pricelist 47, cash_ledger 52,
                  cash_register_shifts 8, employees 24, wage_agreements 8,
                  payroll_periods 146, company_payments 41, work_assignments 33,
                  shift_hours 12, finance_documents 1220, finance_document_lines 401,
                  finance_payment_events 246, finance_expenses 2269,
                  finance_suppliers 199, finance_bank_transactions 0,
                  finance_expense_matches 0, finance_sync_runs 9, finance_audit_log.
SECRETS/AUTH:     Secrets ב-.env מקומי + Environment ב-Render (אין Vault).
                  אימות CRM עם sessions; הרשאות דרך userAccess.js —
                  hasSensitiveAccess(user, 'finance') שומר על כל ה-finance router.
                  אין עדיין רול "רו״ח read-only" — ייבנה.
ICOUNT קיים:      server/icount.js — API v3, Bearer token (ICOUNT_API_TOKEN).
                  ממומש דו-כיווני חלקי: push: client/create+update (ensureClient),
                  doc/create (invrec/offer/refund), doc/cancel, cc/refund,
                  paypage + IPN webhook (icount/webhook + חתימת HMAC). pull:
                  doc/search, doc/info, expense/search, cc/transactions.
                  backfill מלא קיים (financeSync.js, חלון 45 יום אינקרמנטלי,
                  ריצה לילית מ-Render Cron). מזהי iCount נשמרים על הרשומות:
                  parents.icount_client_id, payments.icount_doc_number/id/url,
                  pos_sales.icount_doc_number. הסליקה עוברת דרך iCount (מסוף
                  Max דרך עמוד תשלום iCount + IPN). אין מודול מלאי בחשבון.
                  אין sandbox נפרד — הגנת סביבה תיבנה בקוד.
שמירת סכומים:     שקלים בנקודה צפה מעוגלת ל-2 ספרות (roundMoney/roundFinance),
                  ברוטו+נטו+מע״מ בשדות נפרדים בטבלאות finance_*. מע״מ 18%
                  (vat.js: VAT_RATE=0.18, netAmount/chargeAmount/vatBreakdown).

# --- נתונים סופיים, אל תסטה מהם ---
בנק:              מרכנתיל דיסקונט (בנק 17). חשבון עסקי, גישת צפייה בלבד.
כרטיס אשראי:      Max (מקס) — כרטיס עסקי.
מייל:             Gmail / Google Workspace. חשבוניות מגיעות לתיבה ארגונית.
מטבע:             ILS. סכומים באגורות (integer), לא float. אף פעם לא float על כסף.
                  [החלטת יישום: בסכמת finance החדשה בלבד — אגורות integer;
                  טבלאות קיימות נשארות בשקלים, המרה בגבול. ראה DECISIONS.md]
מע״מ:             עוסק מורשה, 18%.
משתמשים:          בעלים (הכול) · Eyal, מנהל ראשי (הכול חוץ משכר) · רו״ח חיצוני (read-only + ייצוא)
תחום:             קיר טיפוס — חוגים לילדים, כניסות חופשיות, מנויים, ימי הולדת, קופה/חנות, מבחני דירוג
```

---

## 1. מי אתה ומה המשימה

אתה סוכן מהנדס תוכנה בכיר שמתמחה ב־fintech ובמערכות ניהול עסקיות. אתה עובד על מערכת ניהול קיימת של **קיר טיפוס** ("קיר בועז") שכוללת: לקוחות ולידים, לוח חוגים, יומן פעילויות, קופה ומכירה, עובדים ומשמרות, ציוד, מבחנים, הצהרות וטפסים, מסוף כניסה. קיים דף "דוחות פיננסיים" בסיסי (KPI cards + גרף עמודות אחד + סנכרון iCount).

**המשימה:** להפוך את הדף הבודד הזה ל**מרכז פיננסי מלא** — מקום אחד שבו יש הכנסות, הוצאות, חשבוניות, תזרים, רווחיות, שכר וספקים; שיודע למשוך נתונים אוטומטית מהבנק, מכרטיסי האשראי וממייל; להתאים ביניהם; ולהציג את הכול ברמת מוצר SaaS מסחרי.

**עקרון על:** אנחנו לא בונים "עוד דשבורד". אנחנו בונים **ספר חשבונות אחד (single source of truth)** שהדשבורד הוא רק תצוגה שלו. כל מספר על המסך חייב להיות ניתן ל־drill‑down עד לשורת התנועה הבודדת שיצרה אותו.

---

## 2. כללי ברזל (non‑negotiable)

1. **אין ספירה כפולה. אף פעם.** זה הכלל הכי חשוב במסמך. ראה סעיף 5.3.
2. **RTL עברית** מלא — כולל כיווניות מספרים, טבלאות, גרפים, ותוויות צירים. ₪ מימין, `tabular-nums` בכל מספר.
3. **הפרדה בין מזומן לצבירה** (cash vs accrual). תזרים = מתי הכסף זז. רווחיות = למתי ההכנסה/ההוצאה שייכת. חייב להיות toggle גלובלי בין שני הבסיסים.
4. **הכל idempotent.** ריצת ייבוא פעמיים לא יוצרת כפילויות. כל תנועה מקבלת `dedupe_hash` יציב.
5. **audit trail על הכול.** מי שינה, מה, מתי, ומה היה לפני. אסור מחיקה קשה של תנועות — רק `voided`.
6. **מע״מ הוא אזרח מדרגה ראשונה.** כל סכום נשמר ברוטו/נטו/מע״מ בנפרד + `vat_deductible_rate`.
7. **אין הזנת סיסמאות בנק בקוד או ב־DB בטקסט גלוי.** ראה סעיף 12.
8. **ביצועים:** טעינת הדף < 2 שניות עם 100K תנועות. אגרגציות דרך materialized views / pre‑aggregated tables, לא חישוב חי על כל טעינה.
9. **אל תשבור כלום קיים.** iCount sync שקיים היום ממשיך לעבוד; אתה עוטף אותו, לא מחליף.

---

## 3. מודל הנתונים (בנה את זה ראשון — Phase 0)

צור סכמה חדשה בשם `finance`. הישויות המרכזיות:

### 3.1 ליבה

| טבלה | תיאור | שדות קריטיים |
|---|---|---|
| `financial_accounts` | חשבון בנק / כרטיס אשראי / קופה / ארנק דיגיטלי | `type` (bank/credit_card/cash/clearing), `institution`, `last4`, `currency`, `is_active` |
| `transactions` | **כל** תנועה גולמית מכל מקור | `account_id`, `external_id`, `booking_date`, `value_date`, `amount` (חתום), `currency`, `raw_description`, `merchant_raw`, `direction`, `kind` (expense/income/transfer/settlement/fee/refund), `status`, `dedupe_hash` |
| `documents` | חשבונית / קבלה / חשבונית מס / זיכוי | `supplier_id`, `doc_type`, `doc_number`, `allocation_number` (מספר הקצאה), `issue_date`, `total_gross`, `vat_amount`, `currency`, `file_url`, `source` (email/upload/icount/manual), `ocr_json`, `confidence` |
| `matches` | קשר **רבים‑לרבים** בין תנועה למסמך | `transaction_id`, `document_id`, `allocated_amount`, `confidence`, `method` (auto/rule/manual), `matched_by`, `matched_at` |
| `suppliers` | ספקים | `name`, `tax_id`, `aliases[]` (שמות בית‑עסק כפי שמופיעים באשראי!), `default_category_id`, `payment_terms`, `is_recurring` |
| `categories` | עץ קטגוריות הוצאה/הכנסה | `parent_id`, `cost_behavior` (fixed/variable/semi), `is_cogs`, `vat_deductible_rate`, `tax_deductible_rate` |
| `ledger_entries` | שורת ספר ראשית — **המקור לכל דוח** | `date`, `amount`, `category_id`, `cost_center_id`, `basis` (cash/accrual), `source_type`, `source_id`, `period` |

### 3.2 מרכזי רווח/עלות (הלב של סעיפים 8–9 בדרישות)

`cost_centers` עם `type` מתוך: `class` (חוג) · `activity` (פעילות ביומן) · `event` (יום הולדת/קבוצה) · `membership` (מנוי) · `pos_product` · `department` (קיר/חנות/קפה) · `overhead`.

כל `ledger_entry` — הכנסה או הוצאה — יכולה להיות משויכת ל־cost center, **כולל פיצול** (`cost_allocations`: entry_id, cost_center_id, ratio). עלויות עקיפות (שכ״ד, חשמל, ביטוח) מוקצות לפי `allocation_driver` שנבחר: שעות שימוש / מ״ר / הכנסה יחסית / ידני.

### 3.3 שכר ועובדים

`payroll_runs`, `payroll_lines` (`employee_id`, `gross`, `employer_national_insurance`, `pension`, `study_fund`, `benefits`, `total_employer_cost`). חשב `effective_cost_per_hour` לכל עובד = עלות מעביד כוללת ÷ שעות בפועל מ־`shifts`. שייך אוטומטית עלות מדריך לחוג/פעילות שהוא הועסק בה לפי לוח המשמרות.

### 3.4 תזרים

`cash_flow_items` — צפי כניסות ויציאות: הוראות קבע של הורים, תשלומים עתידיים בכרטיס (עסקאות תשלומים!), שכר, שכ״ד, מע״מ, מקדמות מס, ביטוח לאומי. כל פריט עם `confidence` ו־`recurrence_rule`.

---

## 4. מקורות נתונים ואינטגרציות

### 4.1 בנק וכרטיסי אשראי — מרכנתיל דיסקונט + Max

- **ספק:** `israeli-bank-scrapers` (npm). שני ה־providers הרלוונטיים נתמכים בו: `mercantile` ו־`max`. עטוף אותם ב־interface אחיד `BankProvider { fetchTransactions(since): RawTxn[] }` כדי שנוכל להחליף בעתיד לספק Open Banking מורשה (רישיון נותן שירות מידע פיננסי) בלי לגעת בלוגיקה.
- **מרכנתיל** דורש שלושה שדות: ת״ז, קוד משתמש וסיסמה (משפחת Discount) — לא רק שם משתמש וסיסמה. אל תניח מבנה credentials אחיד לשני הספקים.
- **Max** מחזיר תנועות פר כרטיס, ו**עסקאות תשלומים** מופיעות כשורות עתידיות. אל תרשום את כולן כהוצאה בחודש הרכישה: התשלום הראשון הוא הוצאה שוטפת, השאר נכנסים ל־`cash_flow_items` כהתחייבות עתידית ידועה. זה מקור נפוץ לעיוות תזרים.
- הרצה: cron יומי בשעות הלילה + כפתור "סנכרן עכשיו". חלון משיכה חופף (45 יום אחורה) כדי לתפוס תנועות שנרשמו באיחור — ה־`dedupe_hash` מטפל בכפילויות.
- **טיפול ב־OTP/2FA:** שני הספקים עלולים לדרוש אימות. כישלון אימות ≠ כישלון ריצה. צור פריט "נדרש אימות" בתיבת הנכנס + התראה, ואל תפיל את הג׳וב.
- ה־scraper הוא **read-only**. אין ולא תהיה שום פעולת ביצוע (העברה, תשלום, שינוי הגדרות) — לא בקוד ולא בעתיד.
- שמור `raw_json` מלא לכל תנועה. תצטרך אותו לדיבוג התאמות ולשינויי פורמט בצד הבנק.
- ה־scraper שביר מטבעו (הבנק משנה HTML). עטוף בניטור: אם ריצה מחזירה 0 תנועות ביום עסקים, זו **שגיאה**, לא "אין תנועות".

### 4.2 מייל → חשבוניות — Gmail / Google Workspace

- **הרשאות:** OAuth עם scope `gmail.readonly` בלבד. מכיוון שזה Google Workspace, העדף **service account עם domain-wide delegation** על תיבה ייעודית — נמנע מריענון טוקנים ידני ומניתוקים.
- **סינון:** label ייעודי (למשל `Invoices`) + תנאי `has:attachment`. בנוסף, כתובת ייעודית שאליה אפשר לעשות forward לחשבונית שהגיעה למקום אחר.
- **Pipeline:** fetch → חילוץ קבצים מצורפים → parsing (קודם שכבת טקסט של ה־PDF, OCR רק כשאין) → חילוץ: ספק, ח.פ., מספר מסמך, **מספר הקצאה**, תאריך, סכום, מע״מ → יצירת `document` עם `confidence`.
- **סנכרון:** Gmail History API עם `historyId` כ־cursor. אם רוצים זמן אמת — Pub/Sub watch, אבל שים לב ש**ה־watch פג כל 7 ימים וחייב חידוש מתוזמן**, ותמיד עם poller גיבוי.
- אותו pipeline בדיוק משרת גם **העלאה ידנית**: drag & drop, צילום מהנייד, ו־forward למייל הייעודי. אל תבנה שלושה מסלולים נפרדים.
- חשבוניות רבות בישראל מגיעות כ**קישור** ולא כקובץ מצורף. טפל גם בזה: זהה דומיינים מוכרים (iCount, חשבונית ירוקה, Morning וכד׳) והורד את המסמך.

### 4.3 iCount — סנכרון דו־כיווני מלא (דרישת ליבה)

iCount הוא **מקור האמת החשבונאי**. המערכת שלנו היא **מקור האמת התפעולי**. הסנכרון חייב להיות מלא ודו־כיווני, לא ייבוא חד־פעמי.

**4.3.1 חלוקת בעלות — קבע את זה מפורשות בקוד ואל תסטה:**

| ישות | מקור אמת | כיוון |
|---|---|---|
| לקוחות / הורים | המערכת שלנו | push → iCount |
| מוצרים ופריטים בקופה | המערכת שלנו | push → iCount |
| **מסמכים** (חשבונית מס, קבלה, זיכוי) | **iCount** | trigger מהמערכת → iCount מנפיק → pull חזרה |
| מספור מסמכים + **מספר הקצאה** | iCount / רשות המסים | pull בלבד — לעולם לא לייצר אצלנו |
| הוצאות וספקים | דו־כיווני עם מיזוג | merge לפי ח.פ. + מספר מסמך |
| קטגוריות הנהח״ש | iCount | pull, ממופה לעץ הקטגוריות שלנו |
| סליקה / תשלומים | iCount (אם הסליקה דרכו) | pull |

**4.3.2 ארכיטקטורה — Outbox, לא fire-and-forget.**
כל אירוע שמחייב מסמך (תשלום בקופה, הרשמה לחוג, חיוב מנוי, אירוע) נכתב לטבלת `icount_outbox` עם `idempotency_key` ייחודי שנגזר ממזהה האירוע. worker מושך, שולח, ומסמן. כשלון → retry עם backoff אקספוננציאלי → dead letter + התראה + כפתור "נסה שוב" בממשק. **אסור שהפקת חשבונית תיכשל בשקט.**

**4.3.3 טבלאות תשתית:**
- `icount_links` — `entity_type`, `local_id`, `icount_id`, `last_synced_at`, `content_hash`, `sync_status`
- `icount_outbox` — `event_type`, `payload`, `idempotency_key` (unique), `attempts`, `last_error`, `status`
- `icount_sync_runs` — יומן ריצות עם cursor, ספירות, ופערים שהתגלו

**4.3.4 משיכה (pull):**
- Polling מתוזמן עם cursor על תאריך עדכון + backfill היסטורי מלא (יש כבר 1,220 מסמכים ו־2,269 הוצאות — ייבא הכול, לא רק מהיום).
- שמור `raw_json` של כל מסמך. משוך גם קובץ ה־PDF ואחסן אצלנו.
- אם iCount תומך ב־webhooks — השתמש, אבל **תמיד עם poller גיבוי**. webhook שאבד = חשבונית חסרה = פער במאזן.

**4.3.5 דחיפה (push):**
- הפקת חשבונית/קבלה מהקופה, מהרשמה לחוג, ומחיוב מנוי — אוטומטית ומיידית.
- מיפוי אמצעי תשלום: מזומן / אשראי / העברה / צ׳ק / ביט → סוגי התשלום של iCount.
- **ביטול = זיכוי, לעולם לא מחיקה.** תיעוד סיבת הזיכוי אצלנו ובמסמך.
- מצב sandbox חובה: בסביבת פיתוח **אסור** להנפיק מסמכים אמיתיים. הגן על זה ברמת הקונפיג, לא ברמת המשמעת.

**4.3.6 יישוב קונפליקטים:**
מסמך שנוצר ידנית ישירות ב־iCount (בעל העסק/רו״ח) חייב להיקלט אצלנו ולהשתייך אוטומטית ללקוח ולמרכז הרווח הנכון — או לנחות בתיבת הנכנס לשיוך ידני. עדכון משני הצדדים באותו חלון → iCount מנצח על מסמכים, אנחנו מנצחים על לקוחות ומוצרים. תעד כל override ב־audit log.

**4.3.7 יישוב יתרות (reconciliation) — קריטריון הקבלה של השלב:**
ג׳וב לילי שמשווה, פר תקופה: סך ההכנסות אצלנו מול סך המסמכים ב־iCount; ספירת מסמכים; סך המע״מ; סך ההוצאות. **כל פער מוצג כפריט בתיבת הנכנס עם הסבר, לא נבלע בלוג.** יעד: אפס פערים בסוף כל חודש.

**4.3.8 מלכודת ספירה כפולה נוספת:**
אם הסליקה של תשלומי ההורים עוברת דרך iCount — ההפקדה המרוכזת שנכנסת לבנק היא **תקבול מרוכז של עשרות קבלות**, בדיוק כמו חיוב אשראי מרוכז. סווג `kind='settlement'`, קשור למחזור הסליקה, ואמת שסכום ההפקדה = סכום הקבלות פחות עמלות. **עמלות הסליקה עצמן הן הוצאה שצריכה להירשם בנפרד** — הן נבלעות היום ורוב העסקים לא רואים אותן.

**4.3.9 פאנל בריאות סנכרון בממשק:**
הרחב את הכרטיס הקיים בראש הדף ("חי · iCount · Notion ארכיון") לפאנל אמיתי: זמן סנכרון אחרון, פריטים ממתינים ב־outbox, שגיאות פתוחות, פערים שהתגלו, וכפתור סנכרון מלא/חלקי. הארכיון ב־Notion — הגדר אם הוא נשאר, ואם כן סנכרן אליו לקריאה בלבד.

---

## 5. מנוע ההתאמה (Matching Engine) — הרכיב הקריטי

### 5.1 מה מותאם למה

```
תנועת בנק / חיוב אשראי  ←→  מסמך (חשבונית/קבלה)
```

יחס **רבים‑לרבים עם הקצאה חלקית**: חשבונית אחת יכולה להיפרע ב־3 חיובים; חיוב אחד יכול לכסות 5 חשבוניות של אותו ספק. אל תבנה יחס 1:1 — זה יתפוצץ תוך שבוע.

### 5.2 אלגוריתם ניקוד

לכל זוג מועמד תן ציון 0–100:
- **סכום** (משקל 40): התאמה מדויקת = מלא; פער עד 1% או עד ₪5 = חלקי; פער מע״מ ידוע = חלקי.
- **תאריך** (25): חלון של −7/+45 יום בין מועד המסמך למועד החיוב (אשראי מחייב באיחור!). דעיכה ליניארית.
- **ספק** (25): fuzzy match בין `merchant_raw` לבין `suppliers.aliases[]`. כל התאמה ידנית שהמשתמש מאשר → **נלמדת ונשמרת כ־alias חדש**. זו הפיצ'ר שהופך את המערכת לחכמה עם הזמן.
- **מזהים** (10): מספר מסמך/אסמכתא שמופיע בתיאור התנועה, ארבע ספרות אחרונות של הכרטיס.

**ספים:** ≥90 → התאמה אוטומטית (עם אפשרות ביטול). 60–89 → "הצעה" עם כפתור אישור בקליק אחד. <60 → נשאר בתיבת הנכנס.

### 5.3 מניעת ספירה כפולה — חובה (דרישה #3 שלך)

- תנועת בנק שהיא **חיוב אשראי מרוכז** מסווגת כ־`kind = 'settlement'` ולעולם **לא** נכנסת לדוח רווח והפסד.
- היא נקשרת ל־`credit_card_cycle` — ומאומתת: `סכום החיוב בבנק == סכום כל התנועות במחזור`. פער = התראה, לא התעלמות.
- ההוצאה מוכרת ברמת **התנועה הבודדת בכרטיס**, לא ברמת החיוב בבנק.
- אותו טיפול להעברות בין חשבונות, הפקדות קופה לבנק, והחזרי עובדים. כלל: `kind='transfer'` → מופיע בתזרים, לא ברווחיות.
- בדוק גם כפילות הפוכה: חשבונית מ־iCount + אותה חשבונית מהמייל = מסמך אחד (dedupe לפי ח.פ. + מספר מסמך + סכום).

### 5.4 תיבת נכנס פיננסית (Financial Inbox) — הרעיון המרכזי בממשק

במקום שהמשתמש "יחפש בעיות", המערכת מציגה **תור משימות** אחד:
- ⚠️ חיוב ללא חשבונית (₪X מע״מ שלא ניתן לקזז)
- 📄 חשבונית ללא חיוב תואם
- 🔀 התאמות מוצעות לאישור (batch approve)
- 🏷️ הוצאות לא מסווגות (יש לך 109 כאלה כרגע)
- 🔁 חיוב חשוד ככפול
- 👤 ספק חדש שדורש הגדרה

יעד: **Inbox Zero פיננסי**. שים counter על הסיידבר.

---

## 6. דוחות וחישובים שצריך לבנות

### 6.1 רווח והפסד (P&L)
מדורג: הכנסות → הנחות/זיכויים → הכנסה נטו → עלויות ישירות (שכר מדריכים, ציוד מתכלה, סחורה בקופה) → **רווח גולמי** → הוצאות תפעול (שכ״ד, חשמל, ביטוח, שיווק, מנויי תוכנה) → **EBITDA** → פחת → **רווח נקי**. עם השוואה לתקופה קודמת ולתקציב.

### 6.2 תזרים מזומנים
בפועל + **תחזית 90 יום** מבוססת: הוראות קבע צפויות של הורים, תשלומים עתידיים באשראי, שכר קבוע, מע״מ ומקדמות. הצג יתרת מינימום צפויה ו־**runway**.

### 6.3 רווחיות לפי מרכז (דרישות 5, 8, 9)
- **פר חוג:** הכנסה מהילדים הרשומים − עלות מדריך (עלות מעביד מלאה × שעות) − הקצאת עקיפות = רווח ושיעור רווח. + **נקודת איזון: כמה חניכים מינימום כדי שהחוג ישתלם.**
- **פר פעילות ביומן:** הכנסה והוצאה של אותו slot.
- **פר מדריך:** הכנסה שנוצרה בשעות שלו ÷ עלותו.
- **פר שעה ופר יום בשבוע:** heatmap של רווח לשעת פעילות → מגלה מתי כדאי לפתוח ומתי סוגרים.
- **פר מוצר בקופה:** לא רק "כמה נמכרו" אלא **רווח גולמי פר מוצר** (מחיר − עלות), Pareto 80/20, ניתוח סל (מה נקנה יחד), ומוצרים מתים.
- **פר אירוע/יום הולדת:** P&L של אירוע בודד.
- **פר מנוי מול כניסה חד־פעמית:** מה יותר רווחי בפועל.

### 6.4 מדדים ניהוליים
LTV של חניך, שיעור נטישה חודשי (churn) ועקומת שימור לפי קוהורט הרשמה, DSO (ימי גבייה), aging של חוב פתוח (0‑30/30‑60/60‑90/90+), אחוז עלות שכר מהכנסה, הכנסה למ״ר, תפוסה מול קיבולת.

---

## 7. מפרט הגרפים (דרישה #1)

בנה ספריית `<FinChart>` אחידה. **כל גרף לחיץ ומוביל לטבלה מסוננת.** מינימום הנדרש:

1. **מפל (Waterfall) של רווח והפסד** — מהכנסה לרווח נקי, לפי חודש/רבעון.
2. **תזרים משולב** — עמודות בפועל + קו תחזית עם רצועת אי־ודאות + קו יתרת מינימום.
3. **Sankey** — מקורות הכנסה (חוגים / כניסות / קופה / אירועים / מנויים) → מרכזי רווח → קטגוריות הוצאה → רווח. גרף אחד שמסביר את כל העסק.
4. **Heatmap** ימים × שעות של הכנסה/רווח/תפוסה.
5. **Scatter רווחיות חוגים** — ציר X מספר חניכים, Y שיעור רווח, גודל בועה = הכנסה, קו נקודת איזון.
6. **Pareto ספקים ומוצרים** — עמודות + קו מצטבר של 80%.
7. **עקומת שימור לפי קוהורט** (retention curve) + טבלת קוהורטים צבעונית.
8. **Aging של חוב פתוח** — עמודות מוערמות לפי גיל חוב, לחיץ ללקוח.
9. **תקציב מול ביצוע** — bullet chart פר קטגוריה.
10. **מגמה עם זיהוי חריגות** — קו + רצועת נורמה + סימון נקודות חריגות.
11. **Treemap הוצאות** לפי קטגוריה→ספק.
12. **דונאט מע״מ** — עסקאות מול תשומות + "מע״מ אבוד בגלל חשבוניות חסרות".

טכנית: Recharts או Visx, אנימציית כניסה עדינה בלבד, tooltip עשיר (ערך + % + שינוי מתקופה קודמת), legend לחיץ לסינון, מצב ריק מעוצב, skeleton בטעינה, ותמיכה ב־RTL בצירים.

---

## 8. UX ועיצוב (דרישה #7)

- **שמור על שפת העיצוב הקיימת**: dark theme, כרטיסי KPI עם accent gradient תחתון, פינות מעוגלות, טאבים עליונים. אל תמציא שפה חדשה — הרחב אותה.
- **הוסף ל־KPI cards**: sparkline של 12 חודשים, דלתא מול תקופה קודמת (%+צבע), ולחיצה שפותחת drill‑down.
- **סרגל סינון גלובלי דביק**: טווח תאריכים, בסיס מזומן/צבירה, סניף/מחלקה, מרכז רווח, ספק, קטגוריה, עובד. + **Saved Views** ("החודש שלי", "חוגי בוקר", "רבעון לרו״ח").
- **טבלאות**: virtualized, מיון וסינון פר עמודה, group by, סיכומי ביניים, בחירה מרובה + פעולות batch, ייצוא CSV/XLSX, עמודות ניתנות להזזה והסתרה.
- **מצב "התאמה"**: מסך שני חלונות — תנועות משמאל, מסמכים מימין, קשירה בגרירה, ואישור המוני של הצעות. תמיכה בקיצורי מקלדת (`A` אשר, `S` דלג, `↑↓`).
- **נייד**: העלאת חשבונית בצילום + אישור התאמות. זה השימוש הכי תדיר בפועל.
- **הרשאות**: מנהל ראשי (הכול), מנהל תפעול (בלי שכר), רו״ח (read‑only + ייצוא).

---

## 9. רעיונות שכדאי לכלול (מה שלא ביקשת ותרצה)

1. **"כסף על הרצפה"** — מונה חי של מע״מ תשומות שלא ניתן לקזז בגלל חשבוניות חסרות, ב־₪. זה ההצדקה הכספית לכל הפרויקט.
2. **בוט רדיפה אחרי חשבוניות** — מייל אוטומטי לספק: "חויבנו ב־₪X בתאריך Y, נשמח לחשבונית מס". עם מעקב.
3. **מנוע חוקים** — "כל חיוב מ־X → קטגוריה Y, ספק Z, מרכז עלות W". נבנה אוטומטית מהתנהגות המשתמש.
4. **זיהוי מנויים שנשכחו** — חיובים חוזרים שאין להם שימוש/ערך. חוסך כסף מיידית.
5. **זיהוי חיוב כפול / חיוב חריג** מאותו ספק.
6. **הכנסות נדחות** — הורה שמשלם שנה מראש: התזרים היום, ההכנסה נפרסת ל־10 חודשים. בלי זה הרווחיות החודשית שלך שקרית.
7. **תקציב והתראות** — חריגה מקטגוריה → התראה בזמן אמת ל־WhatsApp/מייל.
8. **סימולטור תמחור** — "מה קורה לרווח אם אעלה מחיר חוג ב־5% ואאבד 3% חניכים".
9. **תחזית עונתית מודעת ללוח העברי** — חופש גדול, חגים, ימי בחירות. זה משנה דרמטית לעסק חוגים.
10. **ציוד כרכוש קבוע** — קיר, מזרנים, אחיזות: פחת, לוח החלפות צפוי, ועלות תחזוקה לשנה. משתלב עם מודול "ציוד לאימונים" הקיים.
11. **מלאי ו־COGS בקופה** — עלות סחורה, פחת, גניבות (shrinkage), נקודת הזמנה מחדש.
12. **ציון אמינות תשלום ללקוח** + workflow גבייה אוטומטי (תזכורת → שיחה → הקפאה).
13. **סגירת חודש** — checklist עם נעילת תקופה, כך שאף אחד לא משנה נתונים אחורה אחרי דיווח מע״מ.
14. **פורטל רו״ח** — לינק read‑only + ייצוא מובנה לתוכנת הנהח״ש.
15. **מוכנות לריבוי סניפים** — גם אם יש אחד היום, `branch_id` בכל טבלה יחסוך מיגרציה כואבת.
16. **מטבע חוץ** — רכישת ציוד מחו״ל: שער, הפרשי שער, מכס ומע״מ ייבוא.
17. **תיעדוף המלצות** — "3 הפעולות שישפרו הכי הרבה את הרווח החודש" מבוסס על הנתונים.

---

## 10. תוכנית עבודה (בצע לפי הסדר, PR נפרד לכל שלב)

| שלב | תוצר | קריטריון קבלה |
|---|---|---|
| **0** | מיפוי הקוד הקיים + סכמת `finance` + מיגרציות | הסכמה עולה ויורדת נקי; דיאגרמת ERD; דוח על מה קיים היום |
| **1** | ingestion בנק + אשראי, dedupe, סיווג `settlement` | סנכרון פעמיים ≠ כפילויות; חיוב אשראי לא נספר כהוצאה — **בדיקה אוטומטית שמוכיחה זאת** |
| **1.5** | **סנכרון iCount דו־כיווני** — outbox, links, pull+backfill, push, reconciliation | backfill מלא של 1,220 המסמכים; הפקת מסמך מהקופה מגיעה ל־iCount עם מספר הקצאה חוזר; ג׳וב היישוב מציג **אפס פערים** על 3 חודשים אחורה |
| **2** | ingestion מייל + העלאה ידנית + parsing | 90%+ מהחשבוניות נקלטות עם ספק+סכום+תאריך נכונים |
| **3** | מנוע התאמה + מסך התאמה + תיבת נכנס | ≥70% התאמה אוטומטית על דאטה אמיתית; unit tests למנוע הניקוד |
| **4** | ספקים, קטגוריות, מנוע חוקים, מע״מ | "109 הוצאות לא מסווגות" יורד ל־0 בתהליך סביר |
| **5** | שכר, עלות מעביד לשעה, שיוך לחוגים/משמרות | רווחיות פר חוג ופר מדריך מוצגת ומאומתת ידנית על 3 דוגמאות |
| **6** | P&L, תזרים + תחזית, מרכזי רווח, קופה | דוחות תואמים למאזן הבנק בפועל ±0 |
| **7** | דשבורד וגרפים חדשים, סינונים, saved views, ייצוא | טעינה <2s; כל KPI ניתן ל־drill‑down; RTL תקין |

---

## 11. איך אני רוצה שתעבוד

- **הריצה אוטונומית** לפי פרוטוקול סעיף 1.5. אל תחכה לאישורים חוץ מעצירות החובה שם.
- **אל תמציא בשקט**: כל מידע חסר → הנחה מפורשת ב־`DECISIONS.md` עם נימוק. הנחה מתועדת אפשר לתקן; הנחה נסתרת עולה חודש עבודה.
- שנה קוד קיים בזהירות; כל שינוי שובר תואמות עוצר ומחכה לי.
- כתוב **טסטים למנוע ההתאמה, למניעת ספירה כפולה, וליישוב מול iCount** — שלושת המקומות שבהם באג עולה כסף אמיתי. שאר הקוד: טסטים איפה שזה זול.
- כל שלב מאחורי **feature flag**, ניתן לכיבוי, ברירת מחדל כבוי בפרודקשן.
- seed data מאונונימז לפיתוח. לעולם לא נתוני אמת של לקוחות בסביבת פיתוח, ולעולם לא הנפקת מסמכים אמיתיים ב־iCount מחוץ ל־sandbox.
- שמור על שפת העיצוב הקיימת (סעיף 8). אל תמציא שפה חדשה תוך כדי ריצה אוטונומית.

---

## 12. אבטחה ופרטיות

- אישורי גישה לבנק/אשראי ב־secret manager (KMS/Vault) בלבד, מוצפנים במנוחה, לא ב־DB בטקסט גלוי, לא בלוגים, לא ב־git.
- הרשאות read‑only בכל מקום שאפשר. אין ביצוע פעולות כספיות.
- OAuth למייל עם ה־scope המינימלי (`readonly`), על label ייעודי.
- הצפנת קבצי חשבוניות ב־storage + URLs חתומים עם תפוגה.
- לוג גישה לנתוני שכר בנפרד (מידע רגיש).
- עמידה בדרישות הגנת הפרטיות הישראלית (תיקון 13) — מיפוי מאגר, מדיניות שמירה ומחיקה, בקרת גישה מתועדת.
- אל תשלח נתונים פיננסיים לשירותי צד ג' לצורך עיבוד ללא אישור מפורש.

---

## 13. תשובות שאתה חייב לקבל לפני הריצה (מלא אותן — אני לא אעצור לשאול)

בהרצה אוטונומית אין למי לפנות באמצע. ענה על אלה בסעיף 0; כל שדה שיישאר ריק יהפוך להנחה של הסוכן ויירשם ב־`DECISIONS.md`:

- באיזה בסיס חשבונאי אנחנו מדווחים היום — מזומן או צבירה? **[לא סופק — הנחה ב-DECISIONS.md: מזומן, כמקובל בעוסק מורשה שירותים; ה-toggle קיים כך שרו״ח יכול להפוך.]**
- האם החוגים משולמים מראש, ולכמה חודשים? **[לא סופק — נלמד מהנתונים: payments קיימים לרוב חודשיים; פריסת הכנסה נדחית נתמכת לכל תשלום רב-חודשי.]**
- איך רשומות היום הכנסות מהקופה מול הכנסות מהרשמות — אותה טבלה או נפרדות? **[מהקוד: payments אחת + pos_sales; שתיהן מפיקות מסמך iCount.]**
- השכר מגיע ממערכת חיצונית (חילן/מיכפל/מלם) או מוזן ידנית? **[מהקוד: מוזן ידנית — payroll_periods + wage_agreements פנימיים. אין ממשק חיצוני.]**
- מי מגדיר קטגוריות הוצאה — אני או רו״ח? קיים עץ קטגוריות? **[מהנתונים: קטגוריות חופשיות מ-Notion + expense_type של iCount; עץ מסודר ייבנה בשלב 4 עם מיפוי מהקיים.]**
- כמה כרטיסי אשראי, ומי מחזיק בכל אחד? יש כרטיס פרטי שמשמש גם לעסק? **[לא סופק — הסכמה תומכת בכמה כרטיסים (financial_accounts); בהתחלה מוגדר Max עסקי אחד.]**

**על iCount במיוחד:** אילו endpoints כבר ממומשים אצלנו ואילו לא? **[ממומשים: client/create+update+info+get_list, doc/create (invrec/offer/refund), doc/cancel, doc/search, doc/info, expense/search, cc/transactions, cc/refund, paypage/get_list + IPN. חסרים: משיכת PDF, קטגוריות הנהח״ש, webhooks יזומים של iCount.]** האם הסליקה של תשלומי ההורים עוברת דרך iCount או ספק אחר? **[דרך iCount — עמוד תשלום + מסוף Max, עם IPN חתום.]** מי מנפיק היום חשבוניות — המערכת, אני ידנית, או רו״ח? **[המערכת — אוטומטית מהקופה ומעמודי תשלום; ידני ב-iCount קורה גם, ונקלט ב-pull.]** האם יש מסמכים שנוצרים רק ב־iCount ולא אצלנו? **[כן — נקלטים ב-pull ומוצגים כ-accounting_only.]** האם קיים חשבון sandbox? **[לא. הגנת סביבה נבנתה בקוד: הנפקה אמיתית רק בפרודקשן + feature flag.]**
