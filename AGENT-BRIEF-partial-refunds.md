# זיכוי יחסי — מה בנוי, מה חסר, ומה צריך להחליט

נכתב 8.8.2026. קרא את זה לפני שנוגעים בזיכויים — כל מה שכאן אומת מול iCount החי,
לא הונח.

---

## התשובה הקצרה לשתי השאלות

**האם מסלול הזיכוי היחסי עובד במלואו?** לא. המנוע עובד, המדיניות מוגדרת, אבל
רק **ציוד** מחובר לכפתור. כרטיסיות — יש מדיניות, יש מנוע, יש נתוני ניצול, אין מסלול.

**האם אפשר לזכות סכום ידני?** לא. בשום מקום. כל מסלולי הזיכוי דורשים שהסכום
המאושר יהיה **בדיוק** ההמלצה של המדיניות, והזיכוי הכללי מבטל מסמך שלם. אין
„זכה 200 ₪ כי החלטתי".

---

## מה עובד היום

### iCount — אומת מול ה-API החי (8.8.26)

| יכולת | מתודה | סטטוס |
|---|---|---|
| איתור חיוב לפי מסמך | `cc/transactions` עם `start_date`/`end_date` | ✅ עובד. `from_date` נדחה עם `empty_query`. מחזיר `cc_bill_log_id`, `docnumber`, `cctotal`, `refunded` |
| זיכוי חלקי לכרטיס | `cc/refund` עם `cc_bill_log_id` + `sum` | ✅ אומת בזיכוי אמיתי של 1.01 ₪ מתוך 1.18 |
| חשבונית זיכוי | `doc/create` עם `doctype: 'refund'` | ✅ סוג מסמך חוקי בחשבון |
| מינימום | — | ⚠️ **מעל שקל אחד.** מתחת לזה נדחה: „סכום החיוב חייב להיות מעל שקל" |
| מסמך אוטומטי | — | ❌ **זיכוי חלקי אינו יוצר מסמך.** אומת: הכסף חזר, הספרים נשארו ריקים |

מתודה שלא קיימת מחזירה `bad_method`; קיימת מחזירה שגיאת ולידציה. אפשר למפות
את ה-API בבטחה בגוף ריק. התיעוד הרשמי חסום מאחורי התחברות (403).

**`cc_bill_log_id` אינו מופיע ב-`doc/info`** ולא מגיע ב-IPN (נבדק — שתי הודעות
לכל תשלום, באף אחת אין אותו). היומן הוא הדרך היחידה, והוא עובד גם על תשלומים ישנים.

### הקוד

- `server/partialRefund.js` — `executePartialRefund`: איתור חיוב → זיכוי לכרטיס →
  חשבונית זיכוי. **הסדר מכוון**: כסף קודם. כישלון בכרטיס לא מייצר מסמך על החזר
  שלא קרה; כישלון במסמך אחרי שהכסף חזר מדווח ב-`document_error` ולא נבלע.
- `server/cancellationPolicies.js` — `suggestedUsageRefund`, שני מצבי יישוב:
  - `pro_rata` — ערך החלק שלא נוצל × אחוז, פחות דמי ביטול. נכון להשכרה.
  - `full_price` — הנוצל מחויב במחיר יחידה מלא, היתרה חוזרת. נכון לכרטיסייה.
- `server/equipmentRefund.js` — מבודד את רכיב הנעליים מהתשלום ומחשב לפי חלון
  ההשכרה שבאלוקציה.
- מסלול מחובר: **ציוד בלבד** — `POST /api/payments/:id/equipment-refund-preview`
  ו-`/equipment-refund`, וכפתור בתיק הלקוח שמנתב אליו אוטומטית.

### המדיניות

| מדיניות | בסיס | תנאים | מוצמדת ל־ |
|---|---|---|---|
| אירועים (ברירת מחדל) | מועד הפעילות | 7 ימים 100% פחות 50 ₪ · 48ש׳–7י׳ 50% · פחות מ-48ש׳ 0% | כל אירוע שלא נבחר לו אחרת |
| ציוד לחוגים | ניצול · יחסי | 100% ממה שלא נוצל, פחות 50 ₪ | תשלומי ציוד (לפי צילום) |
| כרטיסיות ומנויים | ניצול · מחיר מלא | הנוצל ב-70 ₪ לכניסה, היתרה חוזרת, **בלי דמי ביטול** | 7 מוצרים |

חלון התחרטות 24 שעות בשלושתן. צילום המדיניות נשמר על העסקה ברגע הרכישה —
אירועים, מכירות דלפק וציוד. שינוי מדיניות אינו משנה החזר של רכישה קיימת.

---

## מה חסר

### 1. כרטיסיות — אין מסלול זיכוי

הכול קיים חוץ מהחיבור:
- הנתונים: `customer_passes` מחזיק `visits_total` ו-`visits_remaining` — בדיוק
  היחידות שהמנוע צריך.
- המדיניות: מוגדרת ומוצמדת.
- המנוע: `suggestedUsageRefund` במצב `full_price`.

**מה לבנות:** מסלול כמו של הציוד — `preview` + `refund` — שקורא את הכרטיס לפי
`sale_id`, לוקח `visits_total`/`visits_remaining`, שולף את הצילום מ-`policy_snapshots`
של המכירה, ומריץ `executePartialRefund`. הכפתור בתיק הלקוח כבר קיים; צריך רק
לנתב אליו כמו שנעשה לציוד.

**שים לב:** מכירת דלפק יכולה להכיל כמה פריטים בחשבונית אחת, בדיוק כמו תשלום
הציוד. צריך לבודד את רכיב הכרטיסייה, לא לחשב על כל המכירה.

### 2. אין זיכוי בסכום ידני

היום כל מסלול דורש התאמה מדויקת להמלצה (`refund_amount_approval_required`).
אין דרך לזכות סכום אחר — לא יותר ולא פחות. כשהמדיניות לא מכסה מקרה, התשובה
היחידה היא לצאת ל-iCount ידנית.

**מה לבנות:** שדה סכום בדיאלוג הזיכוי, שממולא מראש בהמלצה וניתן לשינוי. כשהסכום
שונה מההמלצה — לדרוש סיבה, לרשום מי אישר, ולסמן את השורה כחריגה ממדיניות.
הביצוע עצמו כבר קיים: `executePartialRefund` מקבל כל סכום.

**החלטה שצריך לקבל:** מי מורשה לחרוג? היום כל מי שיש לו גישה לתיק הלקוח יכול
לזכות. שווה לשקול הגבלה לבעלים.

### 3. שני המסלולים לא נבדקו מול חיוב אמיתי

`cc/refund` כן — 1.01 ₪ הוחזרו בפועל. אבל אף השכרה ואף כרטיסייה לא זוכו דרך
המערכת. **לפני שסומכים על זה: זיכוי ניסיון קטן על כל מסלול**, ובדיקה שחשבונית
הזיכוי אכן נוצרה.

### 4. הזיכוי החלקי של 1.01 ₪ מ-8.8 חסר מסמך

נעשה כבדיקה, לפני שיצירת המסמך נבנתה. הכסף חזר, אין חשבונית זיכוי. צריך להשלים
ידנית או לתאם מול רואה החשבון.

---

## מלכודות שכבר עלו — אל תיפול בהן שוב

- **`Number(x) ?? 100` אינו תופס NaN.** זה הפך „אין החזר מעל X% ניצול" ל-0
  וחסם כל זיכוי של כרטיסייה שנגעו בה. תוקן, אבל התבנית קיימת במקומות נוספים.
- **`columnMapper` הופך מחרוזת ריקה ל-null.** בעמודה NOT NULL זה מפיל את
  השמירה — ברירת המחדל של העמודה חלה רק כשהיא מושמטת. `keepEmpty` מטפל בזה;
  יש 69 עמודות טקסט NOT NULL בסכמה.
- **חודשים קלנדריים אינם שווי אורך.** חישוב יחסי לינארי נותן 3.02 מתוך 5.
  מעגלים לחצאי יחידה — אותה גרנולריות שבה תומחר המוצר.
- **`cc/refund` בלי `sum`** עלול לזכות את מלוא החיוב. אף פעם לא לקרוא לו עם
  מזהה אמיתי בלי סכום.

---

## איך לבדוק בלי לשרוף כסף

תשלום ניסיון של 1.18 ₪ ← זיכוי חלקי של 1.01 ₪. זה מעל המינימום, ומאפשר לראות
גם את ההחזר וגם את המסמך. `cc/transactions` מציג את היומן בקריאה בלבד.
