# הקשר מערכת — My Wall CRM

מסמך חי. מעדכנים אותו כשיש שינוי משמעותי במטרות, במה שעובד, או בהחלטות חשובות.
הסוכן אמור לקרוא אותו בתחילת כל שיחה.

---

## מה אנחנו בונים

מערכת ניהול תפעולית לקיר הטיפוס **My Wall**.
המערכת מנהלת לידים ולקוחות, חוגים, נוכחות, הצהרות בריאות, תשלומים, הודעות WhatsApp ותפעול יומיומי.

שני תפקידים עיקריים:
- **מנהל** — גישה מלאה, כולל דוחות מכירה, סנכרון מלאי, ומסמכי חיוב
- **צוות** — לידים, חוגים, נוכחות, מסוף כניסה, הצהרות, מכירה בדלפק וניקוב כרטיסיות (בלי דוחות מלאים)

---

## מטרות

1. לרכז במקום אחד את כל התפעול של הקיר
2. להקטין עבודה ידנית (נוכחות, דיוור, תשלומים, קליטת לקוחות)
3. לחבר WhatsApp בצורה יציבה וישירה מול Meta
4. לשמור נתונים בצורה מאובטחת ב-Supabase
5. לאפשר לצוות לעבוד מהר ביום-יום בלי להסתבך

---

## מה כבר עובד

- דשבורד כללי
- מסוף כניסה מהיר
- לקוחות ולידים (הורים + מתאמנים, תיק לקוח, שיחות)
- לוח חוגים ונוכחות (כולל יצירה יומית אוטומטית)
- יומן — אירועים נפרדים מלוח החוגים (ימי הולדת, טיולים, בתי ספר, חברות) עם סנכרון דו־כיווני ליומן גוגל בשם „יומן”; אפשר להציג שכבות מיומנים נוספים (משפחתי וכו׳) ולערוך בהם אירועים כשיש הרשאת כתיבה בגוגל; גרירת אירוע ליום אחר, ובתצוגת שבוע גם הזזה והארכת שעות בגרירה; ביטול פעולה אחרונה ביומן עם קיצור המקלדת הרגיל
  - אירוע רב־יומי: שדה `end_date` (כולל) + אותן שעות כל יום (`start_time`/`end_time`); בתצוגת חודש/שבוע האירוע מופיע בכל יום בטווח עם השעות היומיות (לא בלוק אחד ענק); הרשמה/מכסה/מזמין נשארים על אירוע אב אחד; בתצוגת שבוע אין גרירה/שינוי שעה למופע רב־יומי (רק עריכה בטופס) כדי לא לבלבל בין יום בודד לכל הטווח
  - סנכרון לגוגל: יום שלם רב־יומי = בלוק all-day עם סוף בלעדי; מתוזמן רב־יומי = אירוע חוזר יומי (`RRULE:FREQ=DAILY;COUNT=N`) עם אותן שעות; במשיכה מגוגל מדלגים על מופעי סדרה מורחבים כדי לא לשכפל את האירוע במערכת
- הרשמה ציבורית לאירועי יומן (MVP): דף הרשמה לפי קישור, מזמין, סטטוס תשלום דמי הזמנה, מכסת משתתפים, תבניות לפי קטגוריה, ווידג׳ט בדשבורד לאירועים שלא שולמו
  - נתיב ציבורי באתר: `/event/:slug` — מחוץ למסך הכניסה של הצוות (כמו הצהרת בריאות והצטרפות); גם שכבת הגנה ב־AuthGate אם הנתיב נכנס בטעות ל־catch-all
  - נקודות שרת ציבוריות בלי כניסה: `GET/POST /api/public/activities/:slug` (ו־`.../register`)
  - קישור למזמין בוואטסאפ נבנה מ־`FRONTEND_URL` / כתובת האתר החי (לא localhost בשליחה אמיתית)
  - שני סוגי תשלום: `payment_status` על האירוע = דמי הזמנה מהמזמין לקיר; תשלום משתתף בדף הציבורי = אופציונלי לפי מחיר + `collect_registration_payment` (קישור סליקה קיים)
  - תבניות (`activity_templates`): קטגוריה קבועה כמחרוזת — `field` = פעילויות שטח, `wall` = אירועים בקיר (לא טבלת קטגוריות נפרדת ב־MVP); זרע התחלתי אידמפוטנטי לפי מזהים קבועים
  - זרימות יצירה: מתבנית (ממלא טופס לעריכה לפני שמירה) או אירוע מותאם ללקוח (טופס ריק עם מחיר/טקסט חופשיים)
  - מזמין מקושר ללקוח במערכת: שדה `host_parent_id` על האירוע; בחירה בחיפוש לפי שם הורה / שם ילד / טלפון; שמירת צילום שם/טלפון/אימייל להיסטוריה ולדף הציבורי; שליחת קישור הרשמה בוואטסאפ לטלפון של הלקוח הנבחר (טקסט חופשי — כפוף לחלון 24ש או תבנית מאושרת)
  - אירועים ישנים עם מזמין בטקסט חופשי בלבד עדיין נפתחים; מומלץ לקשר ללקוח לפני שליחה
  - מייל אישור הרשמה: דרך Resend אם מוגדר `RESEND_API_KEY`; אחרת נרשם בלוג בלבד
  - הרשמה קבוצתית מלאה: הזמנה אחת כוללת הורה משתתף לפי בחירה וכמה ילדים; כל אדם נספר במכסה, ונוצרות רשומת משתתף והצהרה חתומה לכל אדם
  - שני מצבי תשלום: `paid_per_participant` מחייב מחיר יחידה כפול מספר המשתתפים; `host_pays` נותן למזמין קישור תשלום פרטי נפרד, בעוד קישור המשתתפים חינמי
  - כל נרשם נכנס למערכת: הורה מאוחד לפי טלפון מנורמל, ילדים נמצאים או נוצרים תחתיו, והורה משתתף נשמר כמבוגר ללא מתאמן
  - הצהרות הפעילות משתמשות בטופס הבריאות והוויתור הרגיל; נשמרים מזהה/שם תבנית, תשובות, חתימה וצילום מלא של נוסח הטופס בזמן החתימה
  - הרשמה בתשלום יוצרת החזקת מקום ל־20 דקות; תשלום שהתקבל מאשר את כל ההזמנה באופן חוזר־בטוח
  - רשימת המשתתפים לצוות נמשכת מחדש מהמסד בפתיחה, בחזרה לחלון, בחזרה ללשונית ובלחצן רענון
- תצוגת נוכחות לילד בתיק לקוח: לוח חודשי עם תגי סטטוס (ממתין למילוי / הגיע / לא הגיע / חליפי / יום חג)
- דיוור WhatsApp: שידורים, קהלים, תבניות, תשובות שמורות
  - ביצירת תבנית: כפתורי משתנים (שם פרטי / משפחה / ילד וכו׳) + מיפוי לכל `{{1}}`/`{{2}}`
  - בשליחה ללקוח ובדיוור: מילוי אוטומטי לפי המיפוי מכרטיס ההורה והמתאמן
  - פילוח לפי קבוצה: כשמסמנים קבוצה — כל הרשומים אליה נכללים, בלי תלות ברשימת התפוצה (חוגים וכו׳)
  - תור „ממתינים לטיפול”: כל הודעה נכנסת מכניסה את הלקוח אוטומטית; רק לחיצה ידנית על „לקוח טופל” מוציאה אותו; הודעה חדשה פותחת את הטיפול מחדש
- קופה וחשבוניות דרך iCount
  - קישור סליקה בוואטסאפ מפיק חשבונית מס קבלה אוטומטית אחרי תשלום (דרך עמוד הסליקה + הודעת שרת חזרה אלינו)
  - כתובת עמוד סליקה: `app.icount.co.il/m/...` (לא `checkout.icount.co.il`) — נבחר לפי מזהה עמוד בחשבון (ברירת מחדל: אימון הכירות, מזהה 45)
  - מסך מכירה בדלפק: מנויים, כרטיסיות, מוצרים; חשבונית מס קבלה / הצעת מחיר; ניקוב כרטיסייה בתיק לקוח ובמסוף כניסה
  - מודל `customer_passes` + `pass_punches` + `pos_sales` — מקור האמת למנויים/כרטיסיות אצלנו
  - מלאי מוצרים: עותק מקומי במחירון (`stock_qty`); מודול המלאי ב-iCount לא זמין בחשבון — סנכרון מנהל מחזיר תמונת מלאי מקומית
  - דוחות מנהל בקופה: מכירות לפי יום/עובד/אמצעי תשלום, מלאי נמוך, מנויים לפוג
  - צוות רואה מסך קופה (מכירה / סגירה / היסטוריה מוגבלת); דוחות, מוצרים ומסמכי חיוב — מנהל בלבד
  - היסטוריית עסקאות קופה עם אפשרות זיכוי (ביטול מסמך במערכת החיוב + ביטול כרטיסיות/מנויים מהעסקה)
  - תבנית וואטסאפ לקישור תשלום (`payment_link`) — ממתינה לאישור מטא; אחרי אישור נשלחת גם כשהחלון סגור
  - בעגלה: שינוי מחיר לפריט, הנחה לפי אחוז או סכום, ומוצר בהתאמה אישית (שם + מחיר)
- מוצרים (לשונית בתוך קופה ומכירה): גריד קטגוריות לחיץ (תמונה/שם), ואז מוצרים בתוך הקטגוריה עם תמונה ומחיר — קיוסק, פעילויות, אימונים, כרטיסיות, ציוד טיפוס וכו׳
- בטיחות: הגדרות בדיקות עם תדירות, חתימה בשם עובד (תאריך ושעה אוטומטיים), היסטוריה לכל בדיקה, בדיקות להיום במסוף כניסה, ודיווח אירועים
- עובדים ומשמרות
  - הסכמי שכר לפי תעריפים (דלפק / חוג / פרטי / בניית מסלולים)
  - מקטעי תשלום (`work_assignments`): שורה לכל עובד עם שעות אישיות, מקושרת לאירוע ביומן או ידנית
  - ביומן: סוג „בניית מסלולים” + שיוך כמה עובדים לאותו אירוע עם שעות שונות (מהשעון או מהתכנון, עם תיקון)
  - לשונית „תשלום חודשי”: סיכום, עריכה ואישור מנהל
  - כרטיס עובד: העלאת קבצי טפסים (חוזה, אישור משטרה, תעודות, צילום ת.ז., טופס 101) לאחסון; הסמכות מקצועיות לעריכה/מחיקה/הוספה (כולל מותאם אישית)
- מבחנים (רמה / אבטחה / הובלה)
- הצהרות וטפסים + מעקב חתימות + PDF לתיק לקוח
- אוטומציות / מסעות לקוח
  - טריגרים: ליד חדש, שינוי סטטוס, תזכורת ביום אימון הכירות, בדיקה יום אחרי אימון הכירות
  - תזכורת בוקר (משעה 8): שולחת להורים של מתאמני הכירות שקבוצתם היום — עם שעה, מדריך והוראות הגעה
  - מעקב בוקר: שולח למי שסומן אתמול בנוכחות כהגיע, וסטטוס המתאמן עדיין אימון הכירות
  - משתנים בהודעה: שם מתאמן, שם הורה, שעה, מדריך, הגעה, קבוצה
  - מחוץ לחלון 24ש נדרשת תבנית מאושרת בוואטסאפ
- טפסים ציבוריים: הצטרפות, הצהרת בריאות, קליטה, פרטיות
- בוט תשובות אוטומטיות (Gemini) עם לוח שעות
  - העברה לצוות לפי מילות מפתח + השהיה אחרי תשובת עובד (מערכת/טלפון)
  - הודעה מחוץ לשעות (פעם ביום)
  - זיכרון שיחה + כרטיס לקוח בפרומפט
  - מצב קהל: כולם / לידים / לקוחות פעילים
  - בסיס ידע, נושאים אסורים, פרטי עסק לעריכה
  - איסוף ליד מדורג, תפריט 1–4, תפריט אינטראקטיבי של Meta (עם נפילה לטקסט)
  - הגנות: קצב, עיכוב, אורך מקסימלי, מילות עצירה/הפעלה מחדש
  - סף ביטחון: UNSURE → העברה לצוות
  - מסך הגדרות מורחב בלשונית דיוור

---

## מה בתהליך / פערים ידועים

- מעבר מ-Wassenger ל-Meta ישיר — לא לנתק את הישן לפני שכל הבדיקות עוברות
- חיזוק אבטחה והחלפת מפתחות שנחשפו בעבר
- חלק מהנתונים בטבלאות ייעודיות וחלק באוסף כללי (`kv_collections`)
- Instagram / Messenger קיימים ברמה בסיסית, פחות בשלים מ-WhatsApp
- אונבורדינג מסמכים: תעודות זהות וקבצי PDF לתיק לקוח
- תבניות הודעות מאושרות חייבות להיות במסד החי (לא רק במטמון מקומי) — אחרת חלונית התקשורת נשארת בלי אפשרויות בחירה
- כפילות כרטיסי הורה לאותו טלפון בפורמטים שונים (למשל `050…` ו-`972…`) — תוקן: המערכת מאחדת אוטומטית לפי טלפון מנורמל בכל יצירה/עדכון, וגם בסריקה בעליית השרת. כרטיס דלק הכפול אוחד ידנית במסד
- הדומיין הציבורי `mywall.co.il` לא פעיל ב-DNS; קישורים ללקוחות חייבים להצביע לכתובת Vercel עד שיחברו דומיין
- דפי האירועים הציבוריים החדשים מוכנים בקוד: הרשמת משתתפים ב־`/event/:slug` ותשלום מזמין ב־`/event-host/:token`. הפרסום ל־Vercel הוחזר לגרסה הקודמת כדי לא לשבור את הזרימה לפני פרסום השרת; יש לפרסם יחד את האתר ואת שרת Render
- מיגרציות ההזמנה הקבוצתית וקישורי תשלום המזמין הוחלו במסד החי ב־25 ביולי 2026
- השרת המקומי משתמש כרגע ב־`SUPABASE_KEY` ולא במפתח תפקיד השרת; נוספה מדיניות תאימות זמנית לטבלת הזמנות ההרשמה. יש להגדיר `SUPABASE_SERVICE_ROLE_KEY` בכל הסביבות ואז להסיר את מדיניות התאימות
- העלאות לשרת ב-Render נכשלו כמה פעמים (`update_failed`) — תוקן בפרסום מחדש עם ניקוי מטמון (יולי 24); השרת החי מעודכן
- טופס הצהרת בריאות ציבורי: מילוי מראש של שם המתאמן לפי קישור — פורסם לאתר החי
- הצהרת בריאות שנחתמה לא תמיד הופיעה בתיק: סטטוס `registered` נשאר אחרי חתימה, והקובץ לא נשמר אוטומטית לתיק האישי — תוקן (זיהוי לפי תאריך חתימה + שמירת קובץ אישור)
- יצירת הסכם שכר חדש נכשלה בשקט: בשרת היו רק שליפה ועדכון, בלי נתיב יצירה — תוקן (נוסף נתיב יצירה, מונע כפילות לאותו עובד, והממשק מציג הודעת שגיאה במקום להיסגר בשקט)

---

## מבנה טכני בקצרה

| שכבה | מה משתמשים |
|---|---|
| אתר | React + Vite |
| שרת | Node + Express |
| כניסה למשתמשים | Supabase Auth |
| מסד נתונים | Supabase (Postgres) |
| מטמון מקומי | `server/db.json` (לא מקור האמת) |
| פריסת API | Render |
| פריסת אתר | Vercel |

כתובת האתר החי כרגע:
`https://client-omega-topaz-35.vercel.app`

הדומיין `mywall.co.il` עדיין לא מחובר ב-DNS — לא להשתמש בו בקישורים ללקוחות.

פורטים מקומיים:
- אתר: `3000`
- שרת: `5000`

תיקיות עיקריות:
- `client/` — ממשק המשתמש
- `server/` — API ולוגיקה
- `database/` — מיגרציות SQL

---

## אינטגרציות

| שירות | תפקיד |
|---|---|
| Supabase | כניסה, מסד נתונים, אחסון קבצים |
| Meta WhatsApp | שליחה וקבלה של הודעות |
| Wassenger | ישן — בתהליך החלפה |
| iCount | תשלומים וחשבוניות |
| Gemini | בוט תשובות אוטומטיות |
| Google Calendar | סנכרון דו־כיווני של מסך היומן |
| Render Cron | יצירת נוכחות יומית |

כתובת API בפרודקשן:
`https://climbing-crm-api.onrender.com`

---

## מסמכי התקנה חשובים

- `README.md` — הפעלה מקומית ובדיקות
- `SUPABASE-SETUP.md` — כניסה, הרשאות, אבטחת טבלאות
- `META-WHATSAPP-SETUP.md` — מעבר ל-WhatsApp של Meta
- `GOOGLE-CALENDAR-SETUP.md` — חיבור יומן גוגל למסך היומן
- `server/.env.example` — תבנית משתני שרת

---

## החלטות חשובות

- Supabase הוא מקור האמת לנתונים
- `db.json` הוא מטמון מקומי בלבד
- WhatsApp עובר לחיבור ישיר מול Meta
- לא לנתק Wassenger מוקדם מדי
- תשובות למשתמש תמיד בעברית פשוטה בלבד — בלי ערבוב אנגלית באותה שורה, בלי תעתיק כמו „פרודקשן”, ובלי ראשי תיבות כמו PDF בתוך משפט עברי (ראו כלל העברית)
- תבניות אירוע ביומן: קטגוריה כשדה מחרוזת קבוע (`field` / `wall`) ולא טבלת קטגוריות — פשוט ויציב ל־MVP; תוויות עברית רק בממשק
- תשלום מזמין (`payment_status` על האירוע) נפרד מתשלום משתתף בדף ההרשמה הציבורי
- קישור תשלום המזמין הוא פרטי ונפרד מקישור הרשמת המשתתפים; אין לשלוח למזמין את קישור המשתתפים במקום קישור התשלום
- מכסה ומחיר בהרשמה בתשלום מחושבים לפי מספר האנשים, כולל הורה משתתף
- מזמין אירוע = לקוח קיים (`host_parent_id` → `parents`); שדות `host_name` / `host_phone` / `host_email` הם צילום ליציבות היסטורית ולתצוגה ציבורית
- דף הרשמה לאירוע הוא ציבורי לגמרי: בלי כניסת צוות; אותו דפוס כמו טפסי בריאות/הצטרפות
- אירוע רב־יומי ביומן = רשומה אחת עם `date` + `end_date` (כולל) ואותן שעות כל יום; לא יוצרים אוטומטית אירוע נפרד לכל יום
- סנכרון גוגל לקייטנה מתוזמנת: חוזר יומי עם COUNT (לא בלוק אחד מיום ראשון 08:00 עד יום אחרון 13:00)

---

## הערות לעבודה עם הסוכן

- לפני שינוי גדול — לקרוא את המסמך הזה ואת המסמך הרלוונטי להתקנה
- לא לשבור פיצ׳רים קיימים בלי סיבה
- לא לעשות commit בלי בקשה מפורשת
- אחרי שינוי משמעותי במערכת — לעדכן גם את המסמך הזה
