# הקשר מערכת — My Wall CRM

מסמך חי. מעדכנים אותו כשיש שינוי משמעותי במטרות, במה שעובד, או בהחלטות חשובות.
הסוכן אמור לקרוא אותו בתחילת כל שיחה.

פירוט מלא של כל פיצ׳ר נמצא במסמך נפרד — לקרוא אותו רק כשעובדים על התחום הרלוונטי:
`docs/FEATURE-DETAILS.md`

---

## מה אנחנו בונים

מערכת ניהול תפעולית לקיר הטיפוס **My Wall**.
המערכת מנהלת לידים ולקוחות, חוגים, נוכחות, הצהרות בריאות, תשלומים, הודעות WhatsApp ותפעול יומיומי.

שני תפקידים עיקריים:
- **מנהל** — גישה מלאה, כולל דוחות מכירה, סנכרון מלאי, ומסמכי חיוב
- **צוות** — לידים, חוגים, נוכחות, מסוף כניסה, הצהרות, מכירה בדלפק וניקוב כרטיסיות (בלי דוחות מלאים)

---

## מטרות

1. לרכז במקום אחד את כל התפעול של הקיר
2. להקטין עבודה ידנית (נוכחות, דיוור, תשלומים, קליטת לקוחות)
3. לחבר WhatsApp בצורה יציבה וישירה מול Meta
4. לשמור נתונים בצורה מאובטחת ב-Supabase
5. לאפשר לצוות לעבוד מהר ביום-יום בלי להסתבך

---

## מה כבר עובד (תקציר — פירוט במסמך הפיצ׳רים)

- דשבורד כללי ומסוף כניסה מהיר
- לקוחות ולידים (הורים + מתאמנים, תיק לקוח, שיחות)
- לוח חוגים ונוכחות (כולל יצירה יומית אוטומטית) + תצוגת נוכחות חודשית בתיק לקוח
- ציוד לאימונים לילדים: מעקב תשלום ומסירה (נעליים / חולצה / מגנזיום), דף תשלום ציבורי, תיק לקוח, טאב בקבוצה ומסך רשימה
- יומן אירועים עם סנכרון דו־כיווני ליומן גוגל, כולל אירועים רב־יומיים
  - תצוגות חודש / שבוע / רשימה; תגית סינון „פעילויות” מאחדת יום הולדת + בית ספר + פעילות חברה (בטופס עדיין בוחרים סוג מדויק); סוג נפרד „חופשה מאימונים” עם תצוגת רשימה נוחה לעריכה
- הרשמה ציבורית לאירועי יומן: דף הרשמה לפי קישור, הרשמה קבוצתית, שני מצבי תשלום (לפי משתתף / מזמין משלם), הצהרות חתומות, תבניות אירועים
- מיתוג מרכזי: פרופיל עסק, מסך הגדרות עסק למנהל
- דיוור WhatsApp: שידורים, קהלים, תבניות (עם משתנים), תשובות שמורות, תור „ממתינים לטיפול”
  - תבניות מערכת לאירועים (טיוטות): קישור תשלום מזמין + קישור למשתתפים — מסומנות בתגית «אירוע»; אחרי אישור מטא נשלחות גם כשחלון 24ש סגור
- קופה וחשבוניות דרך iCount: מכירה בדלפק, מנויים וכרטיסיות, קישורי סליקה, זיכויים, דוחות מנהל, מלאי מקומי
- מוצרים: קטגוריות ומוצרים עם תמונות ומחירים
- בטיחות: בדיקות תקופתיות עם חתימה והיסטוריה, דיווח אירועים
- עובדים ומשמרות: הסכמי שכר, מקטעי תשלום, תשלום חודשי, כרטיס עובד עם קבצים
- מבחנים (רמה / אבטחה / הובלה)
- הצהרות וטפסים + מעקב חתימות + קובץ אישור לתיק לקוח (תוקף עד סוף יולי של שנה זוגית)
- אוטומציות / מסעות לקוח (ליד חדש, תזכורות אימון הכירות, מעקב בוקר)
- טפסים ציבוריים: הצטרפות, הצהרת בריאות, קליטה, פרטיות
- בוט תשובות אוטומטיות (Gemini) עם לוח שעות, העברה לצוות והגנות
- הבוט מציג מקומות פנויים בקבוצות ויודע לשבץ לרשימת המתנה לפי בקשה
- הבוט לא מוסר מחירים (שאלת מחיר → העברה לצוות) עד שיחברו מחירון חי

---

## מה בתהליך / פערים ידועים

- מעבר מ-Wassenger ל-Meta ישיר — לא לנתק את הישן לפני שכל הבדיקות עוברות
- חיזוק אבטחה והחלפת מפתחות שנחשפו בעבר
- חלק מהנתונים בטבלאות ייעודיות וחלק באוסף כללי (`kv_collections`)
- Instagram / Messenger קיימים ברמה בסיסית, פחות בשלים מ-WhatsApp
- אונבורדינג מסמכים: תעודות זהות וקבצים לתיק לקוח
- תבניות הודעות מאושרות חייבות להיות במסד החי (לא רק במטמון מקומי) — אחרת חלונית התקשורת נשארת בלי אפשרויות בחירה
- הדומיין הציבורי `mywall.co.il` לא פעיל ב-DNS; קישורים ללקוחות חייבים להצביע לכתובת Vercel עד שיחברו דומיין
- דפי האירועים הציבוריים החדשים מוכנים בקוד (`/event/:slug`, `/event-host/:token`); יש לפרסם יחד את האתר ואת שרת Render
- השרת המקומי משתמש כרגע ב־`SUPABASE_KEY` ולא במפתח תפקיד השרת; יש להגדיר `SUPABASE_SERVICE_ROLE_KEY` בכל הסביבות ואז להסיר את מדיניות התאימות הזמנית
- שיפורי מהירות (יולי 25) ועמידות שכבת התקשורת — פורסמו לשרת החי ולאתר החי
- השרת בענן עדיין בתוכנית חינמית ויכול להירדם בלי תנועה; מומלץ לשדרג לתוכנית בתשלום
- מומלץ להוסיף ניטור חיצוני שקורא כל דקה לנתיב הבריאות עם בדיקה מעמיקה
- בסביבה המקומית עדיין מוגדר `SUPABASE_KEY` — כדאי להחליף ל־`SUPABASE_SERVICE_ROLE_KEY` (בשרת החי כבר מוגדר נכון)
- שיפור מהירות לסוכן (יולי 25): מטמון מקומי וסקריפטי בדיקה הוצאו מאינדקס/חסימה; סקריפטי שורש עברו לארכיון

---

## עמידות שכבת ההודעות (יולי 25)

הודעה שנכנסת נשמרת במסד לפני שהלקוח נכנס לתור „ממתינים לטיפול”.
אם השמירה נכשלה, הלקוח לא נכנס לתור, נשלחת בקשה לשירות החיצוני לשלוח שוב, וההודעה נשמרת שוב ברקע.

- טבלת ההודעות במסד היא מקור האמת היחיד לשיחה
- יומן ההודעות במטמון המקומי הוא רק מראה, ונבנה מחדש מהמסד בעליית השרת
- הודעה חוזרת עם אותו מזהה חיצוני נשמרת פעם אחת בלבד
- הודעה שמפנה לכרטיס לקוח שנמחק נשמרת בלי שיוך, ולא נאבדת
- כשחלון השיחה מפגר מול כרטיס הלקוח, השיחה נטענת מהמסד ומוצג באנר לצוות

שם התיקייה של שכבת הכתיבה:
`server/channels/messageStore.js`

בדיקת עמידות מול המסד החי:
`npm run verify:conversations`

תיקונים שהושלמו מתועדים בסוף `docs/FEATURE-DETAILS.md`.

---

## מבנה טכני בקצרה

| שכבה | מה משתמשים |
|---|---|
| אתר | React + Vite |
| שרת | Node + Express |
| כניסה למשתמשים | Supabase Auth |
| מסד נתונים | Supabase (Postgres) |
| מטמון מקומי | `server/db.json` (לא מקור האמת) |
| פריסת API | Render |
| פריסת אתר | Vercel |

כתובת האתר החי כרגע:
`https://client-omega-topaz-35.vercel.app`

הדומיין `mywall.co.il` עדיין לא מחובר ב-DNS — לא להשתמש בו בקישורים ללקוחות.

פורטים מקומיים:
- אתר: `3000`
- שרת: `5000`

תיקיות עיקריות:
- `client/` — ממשק המשתמש
- `server/` — API ולוגיקה
- `database/` — מיגרציות SQL
- `_archive/` — קבצים ישנים שהוצאו מהדרך (לא בשימוש, לא באינדקס)

---

## אינטגרציות

| שירות | תפקיד |
|---|---|
| Supabase | כניסה, מסד נתונים, אחסון קבצים |
| Meta WhatsApp | שליחה וקבלה של הודעות |
| Wassenger | ישן — בתהליך החלפה |
| iCount | תשלומים וחשבוניות |
| Gemini | בוט תשובות אוטומטיות |
| Google Calendar | סנכרון דו־כיווני של מסך היומן |
| Render Cron | יצירת נוכחות יומית |

כתובת API בפרודקשן:
`https://climbing-crm-api.onrender.com`

---

## מסמכים חשובים

- `docs/FEATURE-DETAILS.md` — פירוט מלא של כל פיצ׳ר + תיקונים היסטוריים
- `README.md` — הפעלה מקומית ובדיקות
- `SUPABASE-SETUP.md` — כניסה, הרשאות, אבטחת טבלאות
- `META-WHATSAPP-SETUP.md` — מעבר ל-WhatsApp של Meta
- `GOOGLE-CALENDAR-SETUP.md` — חיבור יומן גוגל למסך היומן
- `server/.env.example` — תבנית משתני שרת

---

## החלטות חשובות

- Supabase הוא מקור האמת לנתונים
- `db.json` הוא מטמון מקומי בלבד
- WhatsApp עובר לחיבור ישיר מול Meta
- לא לנתק Wassenger מוקדם מדי
- תשובות למשתמש תמיד בעברית פשוטה בלבד — בלי ערבוב אנגלית באותה שורה, בלי תעתיק כמו „פרודקשן”, ובלי ראשי תיבות כמו PDF בתוך משפט עברי (ראו כלל העברית)
- תבניות אירוע ביומן: קטגוריה כשדה מחרוזת קבוע (`field` / `wall`) ולא טבלת קטגוריות — פשוט ויציב ל־MVP; תוויות עברית רק בממשק
- תשלום מזמין (`payment_status` על האירוע) נפרד מתשלום משתתף בדף ההרשמה הציבורי
- קישור תשלום המזמין הוא פרטי ונפרד מקישור הרשמת המשתתפים; אין לשלוח למזמין את קישור המשתתפים במקום קישור התשלום
- מכסה ומחיר בהרשמה בתשלום מחושבים לפי מספר האנשים, כולל הורה משתתף
- מזמין אירוע = לקוח קיים (`host_parent_id` → `parents`); שדות `host_name` / `host_phone` / `host_email` הם צילום ליציבות היסטורית ולתצוגה ציבורית
- דף הרשמה לאירוע הוא ציבורי לגמרי: בלי כניסת צוות; אותו דפוס כמו טפסי בריאות/הצטרפות
- אירוע רב־יומי ביומן = רשומה אחת עם `date` + `end_date` (כולל) ואותן שעות כל יום; לא יוצרים אוטומטית אירוע נפרד לכל יום
- סנכרון גוגל לקייטנה מתוזמנת: חוזר יומי עם COUNT (לא בלוק אחד מיום ראשון 08:00 עד יום אחרון 13:00)

---

## הערות לעבודה עם הסוכן

- לפני שינוי גדול — לקרוא את המסמך הזה ואת המסמך הרלוונטי להתקנה
- פרטים עמוקים על פיצ׳ר ספציפי — במסמך הפיצ׳רים, לא כאן
- לא לשבור פיצ׳רים קיימים בלי סיבה
- לא לעשות commit בלי בקשה מפורשת
- אחרי שינוי משמעותי במערכת — לעדכן את התקציר כאן ואת הפירוט במסמך הפיצ׳רים
