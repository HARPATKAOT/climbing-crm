# מסך "עובדים ומשמרות" — סיכום השדרוג להעברה לסביבת פיתוח אחרת

תאריך: 2026-08-04. כל השינויים בצד הלקוח בלבד — אין שינוי בשרת, אין מיגרציית DB,
ואין שדה חדש (ולכן אין צורך בעדכון allowlist של supa.js).

## קבצים שנגעו

| קובץ | מה |
|---|---|
| `client/src/components/Employees.jsx` | כל השינויים במסך |
| `client/src/utils/roleIcons.js` | **חדש** — מיפוי תפקיד → אייקון |

לא נדרשה התקנת חבילה: כל האייקונים מ-`lucide-react` שכבר מותקן (0.344.0).

## 1. תיק העובד — ארבע לשוניות

הבעיה: התיק היה גלילה אחת ארוכה, והסכם השכר בכלל לא היה נגיש ממנו.

- state חדש: `drawerTab` (`file | wage | shifts | certs`), מתאפס ל-`file` בכל
  פתיחה של עובד אחר (`useEffect` על `selectedEmployee?.id`).
- **תיק אישי** — התקשרות, פרטים אישיים, בנק, התראות וואטסאפ.
- **הסכם שכר** — `MonthlyPayCard` (מה נצבר החודש) + `EmployeeWageAgreementCard`
  (חדש: ההסכם עצמו, כל התעריפים, נסיעות, ומה חסר תעריף).
- **משמרות** — `ShiftJournalCard` + `ClassAttendanceSummary`.
- **תעודות ומסמכים** — טפסים ואישורים + תפקידים והסמכות (עם כפתור עריכה).
- פונקציה חדשה `openWageForEmployee(emp)`: פותחת את `WageFormModal` עם ההסכם
  הקיים, ואם אין — עם טופס ריק שכבר מקושר לעובד (`{ employee_id }` בלי `id`,
  ואז `handleSaveWage` עושה POST). זה החיבור שהיה חסר מהתיק להסכם.

## 2. תשלום חודשי — פירוט אמיתי + סינון

- `payrollBreakdown` (useMemo): לכל עובד — שורות העבודה בחודש, פירוק לפי תפקיד
  (`summarizeByRole`), הסכומים (`summarizeWork`), כמה שורות לא מאושרות, והאם
  "עבד החודש".
- כרטיס לכל עובד מציג שורה לכל סוג עבודה: `הפעלת קיר · 6 ש׳ × ₪45 → ₪270`,
  אחריהן נסיעות וסה"כ. לחיצה על השם פותחת את התיק ישירות בלשונית השכר.
- `payrollWorkedOnly` (ברירת מחדל `true`) — כפתורי "עבדו החודש" / "כל העובדים".
- סכום כל החודש מוצג בכותרת הלשונית.
- עובד עם שעות שעון בלבד (בלי שורות עבודה) מסומן במפורש בכרטיס שלו.

## 3. שעון נוכחות — מה זה ומה נעשה

**ההסבר:** רישום ידני של כניסה ויציאה בזמן אמת. השעות שלו נכנסות לשכר **רק**
לעובד שאין לו אף שורת עבודה באותו חודש (fallback ב-`employeeShiftStats`). מי
שמשובץ בלוח המשמרות מקבל שכר לפי שורות העבודה, והשעון לא משפיע עליו — לכן זה
כלי עזר, לא מקור האמת של השכר.

מה שונה: רשת של כרטיס לכל עובד פעיל (20+) הוחלפה ב-
1. כרטיס הסבר קצר בראש הלשונית;
2. "עכשיו במשמרת" — רק משמרות פתוחות (`openShifts` useMemo), עם טיימר וכפתור יציאה;
3. שורת רישום כניסה אחת (בחירת עובד + סוג פעילות + כפתור), עם `clockInEmployee` state;
4. טבלת ההיסטוריה נשארה כפי שהייתה.

הלוגיקה של `handleClock` ו-`/api/shifts/clock-in|clock-out` לא נגעו בה.

## 4. לשונית "עובדים" (לשעבר "עובדים קבועים")

- שם הלשונית: **עובדים**.
- טור **הסמכות** חדש — אייקונים בלבד, עם `title` לשם המלא.
- **מקרא** לחיץ מעל הטבלה: כל תפקיד עם האייקון שלו; לחיצה מסננת לפיו (ולחיצה
  שנייה מבטלת). בנוסף יש dropdown "כל ההסמכות" — `empFilterRole` state,
  והרשימה (`certOptions`) היא תפקידי הקטלוג + כל הסמכה שהוזנה ידנית.
- **תלוש / חשבונית**: קומפוננטה משותפת `PaymentMethodBadge` — תלוש = כחול עם
  אייקון `Receipt`, חשבונית = כתום עם `FileText`. משמשת גם בטבלת הסכמי השכר
  ובכותרת התיק.
- **טלפון** לחיץ: כפתור חיוג (`tel:`) + כפתור וואטסאפ (`wa.me`, המרה ל-972 דרך
  `waNumber()`). לחיצה עליהם לא פותחת את התיק (`stopPropagation`).

## 5. אייקונים לסוגי עבודה — `client/src/utils/roleIcons.js`

`roleIcon(role, key)` ממופה **לפי מפתח התפקיד בשרת** (`SYSTEM_ROLE_KEYS`) ולא לפי
התווית, כדי ששינוי שם של תפקיד לא יאפס לו את האייקון; התווית היא רק גיבוי.
תפקיד לא מוכר מקבל `Briefcase`.

| תפקיד | אייקון |
|---|---|
| הדרכת חוג | `GraduationCap` |
| עוזר מדריך | `HandHelping` |
| הפעלת קיר | `Mountain` |
| הדרכת סנפלינג | `ArrowDownToLine` |
| שיעור פרטי | `User` |
| בונה מסלולים | `Route` |
| דלפק | `ConciergeBell` |
| נסיעות | `Car` |

בשימוש ב: טור ההסמכות והמקרא, טופס הסכם השכר, כרטיס ההסכם בתיק, פירוט התשלום
החודשי, `MonthlyPayCard`, וטבלת הסכמי השכר.

## 6. הסכם השכר — רק מה שרלוונטי

- ב-`WageFormModal` מוצגות רק שורות התעריף של התפקידים שהעובד מוסמך אליהם, ובנוסף
  כל תפקיד שכבר יש לו סכום שמור (אחרת אי אפשר היה לראות או לבטל אותו). קישור
  "הצג את כל התפקידים (N)" פותח את השאר. ה-state `rates` נשאר מלא — הסינון הוא
  בתצוגה בלבד, ולכן שמירה לא מוחקת תעריף מוסתר.
- עובד בלי תפקידים מקבל הודעה שמפנה לסמן לו תפקידים בתיק.
- בטבלת הסכמי השכר: עמודת "נסיעות ליום" בוטלה — הנסיעות הן שורה נוספת בתוך
  עמודת התעריפים (`RateLine`, קומפוננטה משותפת).
- מ-`MonthlyPayCard` הוסרו "התעריפים שלו" והאזהרה על תפקיד בלי תעריף — הם היו
  כפילות של כרטיס ההסכם שמתחתיו באותה לשונית.

## איך זה נבדק

`npx vite build` עובר נקי. בנוסף נבנה harness זמני (vite על פורט 3107, קומפוננטת
`Employees` לבדה ב-`MemoryRouter`, `window.fetch` מוחלף בנתוני דמה) — ונבדקו בדפדפן:
ארבע לשוניות התיק, פתיחת טופס ההסכם מהתיק, הסינון לפי הסמכה, האייקונים לכל תפקיד,
כפתורי הטלפון, ופירוט התשלום החודשי. קבצי ה-harness נמחקו אחרי הבדיקה.

**לא נבדק:** מול השרת האמיתי ונתוני האמת — רק מול נתוני דמה.

## מה נשאר פתוח / רעיונות המשך

- לשונית "תעודות והסמכות" הראשית במסך חופפת עכשיו חלקית ללשונית התעודות בתיק
  ולטור ההסמכות החדש — שווה לשקול לאחד.
- שעון הנוכחות עדיין מאפשר כניסה לכל עובד פעיל; אפשר לצמצם למי שמשובץ היום.
- אין עדיין ייצוא של התשלום החודשי (CSV / דוח להנהלת חשבונות).
