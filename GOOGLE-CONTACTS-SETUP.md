# חיבור אנשי הקשר בגוגל

המערכת כותבת את הלקוחות לאנשי הקשר של חשבון הגוגל, כדי שבשיחה נכנסת יופיע מי
מתקשר במקום מספר בלבד.

## איך נראה איש קשר

| מי | שם בטלפון |
|---|---|
| הורה עם ילדים | `חוג פעיל - דנה כהן - יובל כהן, נועה כהן` |
| הורה בלי ילדים רשומים | `ליד חדש - אבי לוי` |
| מתאמן עם מספר משלו | `מטפס - יובל כהן` |

הסטטוס נמצא בהתחלה בכוונה: מסך השיחה הנכנסת חותך את סוף השורה, ומה שראשון נשאר
גלוי. הסטטוס המוצג הוא המתקדם ביותר במשפחה — הורה עם ילד אחד בחוג פעיל ואחד
בארכיון יופיע כ„חוג פעיל”. ילדים בארכיון לא מופיעים ברשימת השמות.

כשלילד אין שם משפחה משלו, מוסיפים את שם המשפחה של ההורה.

## מה קורה מתי

- כל שינוי במערכת שנוגע לשם או לסטטוס (עדכון סטטוס, עריכת לקוח, הוספת ילד,
  קליטת ליד, מחיקה) מפעיל סנכרון קצר זמן אחרי השינוי. שינויים רצופים מתאחדים
  לסנכרון אחד.
- משימה מתוזמנת רצה כל חצי שעה כרשת ביטחון, למקרה שהשרת היה ישן או מנותק.
- אפשר גם ללחוץ „סנכרון עכשיו” במסך הגדרות עסק.

## הסטטוס אצל הלקוח

בשורת הכפתורים שמעל השיחה, ליד חלון הוואטסאפ, מופיע תג שאומר איפה הלקוח הזה
עומד מול אנשי הקשר:

| התג | מה זה אומר |
|---|---|
| איש קשר מסונכרן | קיים בגוגל, בדיוק בשם שהתבנית מכתיבה |
| ממתין לסנכרון | עוד לא נוצר בגוגל — הסנכרון הבא ייצור אותו |
| ממתין לעדכון | קיים, אבל השם או המספר בגוגל שונים מהתבנית |
| אין מספר לסנכרון | אין מספר תקין, ולכן הלקוח לא נכנס לאנשי הקשר |
| אנשי קשר: לא מחובר | אין חיבור לחשבון גוגל |

מעבר עם העכבר מראה את השם המלא שאמור להיות בטלפון, ובמצב „ממתין לעדכון” גם את
השם שיושב שם עכשיו. לחיצה בודקת מחדש מול גוגל.

רשימת אנשי הקשר נקראת מגוגל פעם בחמש דקות ומשותפת לכל הלקוחות, כדי שפתיחת לקוח
לא תייצר פנייה לגוגל בכל פעם. סנכרון מאפס את המטמון.

הסנכרון חד־כיווני: מהמערכת לגוגל. עריכה ידנית של איש קשר מסונכרן תידרס בסנכרון
הבא.

## הכנה בגוגל (חד־פעמי)

משתמשים באותו פרויקט גוגל של יומן גוגל, ובאותם `GOOGLE_CLIENT_ID` ו־
`GOOGLE_CLIENT_SECRET`. צריך להוסיף שני דברים:

1. **להפעיל את People API** — בקונסולת גוגל, במסך הספרייה, להפעיל
   `Google People API` לפרויקט.
2. **להוסיף כתובת הפניה** — במסך פרטי הכניסה, אצל אותו מזהה לקוח, להוסיף
   לכתובות ההפניה המורשות:

   ```
   https://climbing-crm-api.onrender.com/api/google-contacts/oauth/callback
   ```

   ולפיתוח מקומי:

   ```
   http://localhost:5000/api/google-contacts/oauth/callback
   ```

3. **הרשאה במסך ההסכמה** — להוסיף את ההיקף `.../auth/contacts`.

החיבור הזה נפרד מחיבור היומן ונשמר בנפרד. חיבור אנשי הקשר לא נוגע בחיבור היומן
ולא להפך.

## משתני סביבה

| משתנה | חובה | תפקיד |
|---|---|---|
| `GOOGLE_CLIENT_ID` | כן | משותף עם יומן גוגל |
| `GOOGLE_CLIENT_SECRET` | כן | משותף עם יומן גוגל |
| `GOOGLE_CONTACTS_REDIRECT_URI` | לא | דריסה של כתובת ההפניה שנבנית לבד |
| `GOOGLE_CONTACTS_DEBOUNCE_MS` | לא | כמה להמתין אחרי שינוי לפני סנכרון (ברירת מחדל 45 שניות) |
| `CRON_SECRET` | לא | נדרש למשימה המתוזמנת |

## מצב הפרסום — מה שומר על החיבור חי

זה הדבר היחיד שמפיל חיבורי גוגל לאורך זמן:

| מצב מסך ההסכמה | מה קורה למפתח הרענון |
|---|---|
| Testing | פג כל 7 ימים. הסנכרון נעצר בשקט |
| Internal (ב־Workspace) | לא פג |
| In production | לא פג |

בחשבון Google Workspace עדיף `User Type: Internal` — בלי אזהרות ובלי אימות מול
גוגל. חשוב: במצב הזה אפשר לחבר רק חשבון מהדומיין של הארגון, לא כתובת ג׳ימייל
פרטית.

## חיבור

1. במערכת: הגדרות עסק ← חיבורים ← אנשי קשר בגוגל ← „חיבור לחשבון גוגל”.
2. לאשר בחשבון הגוגל שמחובר לטלפון.
3. אחרי החזרה, סנכרון ראשון רץ אוטומטית.

כל אנשי הקשר נכנסים לקבוצה בשם **קיר בועז**. אנשי קשר אישיים שלא נוצרו על ידי
המערכת לא נוגעים בהם.

## איך המערכת יודעת מי שלה

כל איש קשר שנוצר נושא שדה מזהה פנימי עם מפתח הרשומה במערכת. בכל סנכרון המערכת
קוראת את אנשי הקשר, מזהה את שלה לפי המזהה הזה, ומחליטה מה להוסיף, לעדכן ולמחוק.
לכן אובדן הגדרות מקומיות לא יוצר כפילויות, וכפילות שנוצרה בטעות נמחקת מעצמה.

## הגנה מפני מחיקה המונית

אם סנכרון היה מוחק יותר ממחצית מאנשי הקשר של המערכת, המחיקה נעצרת ומדווחת במסך
במקום להתבצע. זה מכסה מצב שבו טעינת הנתונים נכשלה והרשימה חזרה ריקה. אחרי בדיקה
שהמחיקה באמת נכונה, אפשר להריץ סנכרון עם `force`:

```bash
curl -X POST https://climbing-crm-api.onrender.com/api/google-contacts/sync -H "Content-Type: application/json" -d '{"force":true}'
```

## ניתוק

„ניתוק” מוחק את החיבור מהמערכת. אנשי הקשר שכבר נוצרו נשארים בטלפון ומפסיקים
להתעדכן. למחוק אותם — למחוק את הקבוצה „קיר בועז” באנשי הקשר של גוגל.
