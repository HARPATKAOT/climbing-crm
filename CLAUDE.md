# climbing-crm

## עבודה מקבילה בכמה סשנים

לפני כל משימה שמשנה קבצים — קרא ל-EnterWorktree ועבוד שם, לא בתיקייה הראשית.
כמה סשנים רצים במקביל על אותו מאגר, ובלי worktree הם דורסים זה את זה.

- לשאלות, קריאת קוד או בדיקה בלבד — אין צורך ב-worktree.
- שם ה-worktree צריך לתאר את המשימה (למשל `fix-cash-reset`), לא שם אקראי.
- בסוף המשימה: commit בתוך ה-worktree, ואז merge ל-main ופוש. לדווח למשתמש
  שהעבודה מוזגה, או אם יש קונפליקט שדורש החלטה שלו.
