# תדריך — מסך האירועים והפעילויות

נכתב 7.8.2026, עודכן באותו יום אחרי שכל שלוש ההחלטות הפתוחות נסגרו ונבנו.
מסמך מצב, לא היסטוריה. מה שנפרס מסומן ✅.

---

## איך אירוע מגיע ללקוח — ארבעה שדות, לא אחד

| שדה | מה הוא באמת קובע |
|---|---|
| `registration_enabled` | קיים דף הרשמה ציבורי עם קישור |
| `show_on_site` | **מפרסמים** — האירוע באתר, והבוט מציע אותו למי ששואל |
| `status` | `open` / `completed` / `cancelled` — האם האירוע מתקיים |
| `participant_registration_slug` | הכתובת הציבורית של דף ההרשמה |

הבוט (`getEvents` ← `upcomingPublicActivities` ב-`server/publicSite.js`) דורש את
**כולם**: לא מבוטל, `show_on_site`, `registration_enabled`, slug, תאריך עתידי,
ומקום פנוי. די שאחד חסר כדי שהאירוע לא יוזכר לעולם.

**התקלה שממנה הכול התחיל:** שני טיולים („טיול לנקיק השחור”, 11.8 ו-14.8) עמדו
ביומן עם הרשמה פתוחה וכתובת ציבורית, והבוט ענה „אין אירועים פתוחים”. חסר להם
`show_on_site` — **ולא הייתה לו תיבה במסך העריכה של היומן בכלל**. התיבה היחידה
שם, „דף הרשמה ציבורי” תחת „מצב תצוגה”, כותבת `registration_enabled` בלבד.

---

## מה כבר נפרס

✅ **בחירה אחת „מי יכול להירשם”** במקום שתי תיבות, כשלוש פילים עם אייקונים
(`ActivitiesCalendar.jsx`, מחלקת `.choice-pill` ב-`index.css`):

```
🔒 סגור   ·   🔗 קישור פרטי   ·   🌐 מפורסם
```

היא כותבת מאחורי הקלעים את `registration_enabled` ו-`show_on_site`, ומתחתיה שורה
שמסבירה מה המצב הנבחר עושה. הפאנל התחתון (`ActivityRegistrationPanel`) מקבל
`hideRegistrationToggle` כדי שלא יציג את אותה החלטה פעם שנייה.

✅ **השדה נוסף לטופס ולשמירה** — לפני כן `show_on_site` לא היה ב-payload, כך
שסימון היה נסגר בלי לשמור כלום.

---

✅ **„מצב האירוע” כשדה לבחירה ירד.** „הסתיים” נגזר מהתאריך ואיש לא צריך לסמן
אותו — ושום קוד בשרת לא קרא אותו אף פעם. „בוטל” אינו בחירה בטופס אלא תוצאה של
פעולת הביטול. במקום התפריט יש פס אדום בראש הטופס כשהאירוע מבוטל, ובו כפתור
„החזרה לפעיל” (הוא לא מבטל זיכויים שכבר בוצעו).

✅ **מחיקה מול ביטול — כפתור אחד שמחליט לבד.** כפתור „מחיקה” שואל קודם את השרת
(`GET /api/activities/:id/cancellation-preview`):

- אף אחד מעולם לא נרשם ואין תשלום מזמין → נמחק כרגיל.
- יש נרשמים, או רק היסטוריית הרשמות → נפתח מסך הביטול. `DELETE` מחזיר 409 גם אם
  מישהו יעקוף את המסך, כך שההגנה בשרת ולא רק בממשק.

רישום *מבוטל* עדיין חוסם מחיקה: מחיקת האירוע הייתה משאירה גם אותו בלי מקור.

✅ **זיכוי המוני — „בטל את האירוע וזכה את כולם”.** מסך אישור אחד מציג כמה נרשמים,
כמה מסמכי זיכוי יבוטלו וכמה כסף חוזר; אישור הסכום הוא תנאי לביצוע
(`POST /api/activities/:id/cancel` עם `approved_amount`, ו-409 אם המספר השתנה
בינתיים). הזיכוי **מלא תמיד** — כלל `organizer_cancelled` שכבר קיים
ב-`cancellationPolicies.js` מחזיר 100% כשאנחנו אלה שביטלנו, ומדרגות מדיניות
הביטול לא חלות.

פרטים שחשוב להכיר:

| מצב | מה קורה |
|---|---|
| הזמנה קבוצתית | מסמך זיכוי אחד לכל הקבוצה, לא אחד לכל שם |
| נרשם בלי תשלום | המקום משוחרר, לא נוגעים בכסף |
| תשלום בלי מסמך ב-iCount | ההרשמה מבוטלת, והשם מופיע ברשימת „להחזיר ידנית” |
| מדיניות שמחזירה חלקית | לא מזוכה אוטומטית — ביטול המסמך היה מחזיר את הכול |
| דמי הזמנה של מזמין | נכללים באותו ביטול |
| זיכוי שנכשל באמצע | דוח „הצליח/נכשל” + „נסה שוב”; מה שהצליח לא ירוץ שוב |

האירוע מסומן מבוטל **לפני** הזיכויים, כדי שגם אם זיכוי נכשל הוא כבר ירד מהאתר
ומהבוט.

✅ **הודעה לנרשמים — לא אוטומטית.** אחרי שהזיכויים רצו מוצג „לשלוח הודעת ביטול
ל-N הנרשמים?”. מי שחלון 24 השעות של Meta סגור אצלו מופיע ברשימה נפרדת של „לא
קיבלו הודעה” — אליו צריך לפנות ידנית.

---

## מה נשאר פתוח

- **ההודעה עצמה** נשלחת כטקסט חופשי בלבד. אין תבנית Meta מאושרת ל„אירוע בוטל”,
  ולכן מי שלא כתב לנו ב-24 השעות האחרונות לא יקבל כלום. תבנית מאושרת תסגור את זה.
- **ביומן גוגל** האירוע המבוטל נשאר, נצבע אפור ומקבל שורה ראשונה „❌ האירוע בוטל”
  בתיאור. הכותרת לא נגעה בכוונה — היא זו שמסתנכרנת בחזרה לשם האירוע.

---

## קבצים

| קובץ | תפקיד |
|---|---|
| `client/src/components/ActivitiesCalendar.jsx` | היומן, טופס האירוע, הבחירה החדשה |
| `client/src/components/ActivityRegistrationPanel.jsx` | דף ההרשמה, תשלום, מדיניות ביטול |
| `server/publicSite.js` | `upcomingPublicActivities` — השער שדרכו האירוע מגיע לבוט ולאתר |
| `server/activityRegistration.js` | `registrationIsOpen`, תפוסה, רישומים פעילים |
| `server/activityRegistrationRefund.js` | תוכניות זיכוי לנרשם ולמארח |
| `server/icount.js` | `cancelDoc` — חשבונית זיכוי + זיכוי אשראי |
| `server/activityCancellation.js` | ביטול אירוע שלם: מי מזוכה, כמה, ומי נשאר ידני |
| `client/src/components/CancelActivityDialog.jsx` | מסך האישור, דוח הזיכויים וההודעה |
| `server/cancellationPolicies.js` | `organizer_cancelled` — הכלל שמחזיר 100% |
| `server/wallOperatingDay.js` | מה נחשב יום פעילות של הקיר |
| `server/scripts/readActivities.js` | סקריפט קריאה בלבד: לכל אירוע בטווח תאריכים, באיזה שער הוא נכשל |

בדיקה מהירה של מה שהבוט רואה:

```bash
cd server && node scripts/readActivities.js 2026-08-01 2026-08-31
```
