# DECISIONS — הנחות והחלטות בריצה האוטונומית (2026-08-15)

כל שורה: החלטה + נימוק. מה שמסומן ⚠️ כדאי שתקרא.

## מודל נתונים
1. **טבלאות חדשות כ-kv collections, לא SQL ישיר.** זה הדפוס הקיים לכל פיצ'ר חדש (OPERATIONAL_TABLES) — rollout מיידי בלי מיגרציה ידנית שאי אפשר להריץ מכאן. קובץ SQL מנורמל נכתב במקביל (`database/20260815_finance_center.sql`) להרצה ידנית בעתיד, כמו שנעשה ל-finance_reporting הקיים.
2. ⚠️ **אגורות רק בטבלאות החדשות.** ההנחיה "סכומים באגורות" סותרת את כל הקיים (שקלים float בכל 100+ הטבלאות). שינוי הקיים = שבירת תאימות שאסורה לפי כלל 9. לכן: שדות `*_agorot` (integer) בסכמת finance החדשה, המרה בגבולות דרך `server/financeMoney.js`. הקיים לא נגעתי.
3. **שמות טבלאות עם קידומת `finance_` במקום סכמת Postgres `finance.`** — kv_collections שטוח, אין סכמות; הקידומת נותנת את אותה הפרדה בפועל.
4. **`matches` נבנה מחדש כרבים-לרבים** (`finance_matches` עם allocated_agorot) לצד `finance_expense_matches` הקיים (1:1) שממשיך לשרת את הטאב הקיים עד ההחלפה בשלב 3. אין מחיקה של הישן.

## חשבונאות
5. ⚠️ **בסיס דיווח: מזומן** (לא סופק בסעיף 13). עוסק מורשה נותן שירותים מדווח כמעט תמיד על בסיס מזומן. ה-toggle צבירה קיים בכל הדוחות, אז אם רו״ח יגיד אחרת — לוחצים כפתור, לא בונים מחדש.
6. **מקדם עלות מעביד: ברירת מחדל 1.28** (ביטוח לאומי מעסיק, פנסיה 6.5%, פיצויים 8.33%, הבראה/חופשה) — ניתן לכיול פר עובד ופר רכיב במסך ההגדרות. אין נתוני שכר אמיתיים של רכיבי מעביד במערכת (payroll הקיים הוא ברוטו בלבד), אז כל מספר הוא הערכה מסומנת ככזו בדוחות.
7. **הכנסה נדחית**: מנוי תקופתי (time_membership) נפרס ליניארית על חלון התוקף; כרטיסייה (punch_card) לפי ניקובים בפועל, והיתרה בפקיעה. זה הכלל החשבונאי המקובל ואין הנחיה אחרת.
8. **פחת לא ממומש בשלב זה** (אין נתוני רכוש קבוע). שורת EBITDA→רווח נקי מוצגת עם פחת 0 ומסומנת.

## אינטגרציות
9. ⚠️ **הנפקת מסמכים נשארת סינכרונית כמו היום** (קופה → invrec מיידי). ה-outbox החדש עוטף אותה כרשת ביטחון: כישלון נרשם ב-outbox עם retry+DLQ במקום להיעלם. שינוי ל-async מלא היה משנה התנהגות קיימת (מסמך לפני שורת מכירה) בניגוד לכלל 9.
10. **אין sandbox ב-iCount** — נבנה שער בקוד: הנפקה אמיתית רק כש-`NODE_ENV=production` וגם flag דלוק; בכל סביבה אחרת ה-client מוחלף ב-mock שרושם ל-log. ההגנה בקונפיג, לא במשמעת.
11. **israeli-bank-scrapers לא הותקן בפועל** — תלות כבדה (דפדפן מלא), והתקנה ב-node_modules משותף הייתה נוגעת בסשנים מקבילים. הקוד טוען אותו דינמית אם קיים; בלעדיו ה-provider מדווח "לא מותקן" ונשאר mock + ייבוא CSV הקיים. הוספה ל-package.json תיעשה עם האישור שלך (חסם B1).
12. **מרכנתיל**: מבנה credentials נפרד (ת״ז+קוד משתמש+סיסמה) כבר במודל — לא מניחים אחידות עם Max.
13. **Notion ארכיון נשאר לקריאה בלבד** — ממשיך להיקלט במיזוג הקיים; שום כתיבה חזרה.

## התאמות וספירה כפולה
14. **חיוב אשראי עסקי (Max) בבנק = settlement תמיד**; זיהוי לפי דפוס תיאור ("מקס"/"MAX"/"לאומי קארד" וכו') + סכום שמתאים למחזור. גם הפקדת מזומן מהקופה וגם התחשבנות סליקת iCount מסווגות settlement/transfer ולא נכנסות לרווחיות.
15. **ספי התאמה 90/60 לפי המפרט** (המנוע הקיים 85/60 נשאר בטאב הישן עד ההחלפה).
16. **חלון תאריכים −7/+45 יום** בין מסמך לחיוב, דעיכה ליניארית, לפי המפרט.

## UI
17. ⚠️ **בלי Recharts.** אין שום ספריית גרפים בקליינט והגרף הקיים ידני. FinChart נבנה כ-SVG טהור — שליטה מלאה ב-RTL, בלי תלות חדשה ובלי לנפח bundle. זו סטייה מהצעת המפרט ("Recharts או Visx") לטובת כלל "אל תמציא שפה חדשה".
18. **המסך הקיים FinancialReports.jsx לא נהרס** — טאבים חדשים נוספים לצידו, רכיבים חדשים תחת `client/src/components/finance/`. צבעי טאבים מקובעים עם `--tab-accent` כדי לא להזיז את מחזור הצבעים.
19. **רול "רו״ח" נוסף כ-preset** ב-DEFAULT_ROLES: צפייה בלבד + finance, בלי hr. הזמנה בפועל = פעולה ידנית שלך במסך המשתמשים.
20. **מסמכים חדשים נשמרים כ-base64 בתוך הרשומה** (כמו attachment_metadata הקיים) ולא ב-storage עם URL חתום — עקבי עם הקיים; מעבר ל-storage מתועד כשיפור עתידי.

## תשובות סעיף 13 שנלמדו מהקוד (לא הנחות)
- קופה מול הרשמות: טבלת `payments` אחת + `pos_sales`; שתיהן מפיקות מסמך iCount.
- שכר: פנימי לגמרי (wage_agreements + payroll_periods), אין חילן/מיכפל.
- סליקה: הכול דרך iCount (עמוד תשלום + IPN). אין מסוף פיזי בדלפק — מזומן או קישור.
- חוגים: אין חיוב חודשי אוטומטי בקוד היום. ההכנסה מחוגים נכנסת כתשלומים יזומים/ידניים. ⚠️ המשמעות: "הוראות קבע של הורים" בתחזית התזרים נבנית מזיהוי דפוס חוזר במסמכי iCount, לא מטבלת הוראות קבע.
