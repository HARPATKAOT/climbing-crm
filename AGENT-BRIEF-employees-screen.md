# הנחיות לסוכן הבא — המשך עבודה על מסך "עובדים ומשמרות"

קרא קודם את `HANDOFF-employees-screen.md` — שם מתועד מה כבר נעשה. המסמך הזה הוא
איך לעבוד, ומה נשאר.

## הקשר

CRM לקיר טיפוס. הלקוח הוא **בעל העסק, לא מתכנת** — אל תסביר לו קוד, אל תחזיר לו
רשימת משימות לביצוע, ואל תשאל שאלה חוסמת אם אפשר להחליט לבד ולציין את ההנחה.
עונים בעברית, קצר (עד ~4 שורות), מתחילים במסקנה.

- לקוח: `client/` — React + Vite, RTL, עברית.
- שרת: `server/` — Express + Supabase.
- המסך המדובר: `client/src/components/Employees.jsx` (~3,700 שורות, קומפוננטה אחת
  עם 6 לשוניות + מגירת תיק עובד).

## כללי ברזל בפרויקט הזה

1. **אין הרצת מיגרציות SQL מהסביבה הזאת, ואין דיפלוי אוטומטי.** Render לא מדפלוי
   לבד. אם שינוי דורש מיגרציה — כתוב אותה לקובץ, אמור ללקוח מפורשות שהיא לא הורצה,
   ואל תניח שהיא רצה.
2. **שדה חדש שנשמר על ישות חייב להתווסף ל-allowlist של העמודות ב-`server/supa.js`**,
   אחרת הוא נשמר לכאורה ופשוט נעלם. זו התקלה הכי שקטה בפרויקט.
3. **סקריפטים בצד שרת חייבים `dotenv`** — בלעדיו הם כותבים רק ל-`server/db.json`
   ומדווחים "נשמר".
4. **`db.set` דורס את המטמון המקומי** — סקריפט שזורע טבלה ימחק אותה מ-`db.json`.
5. **git**: בתיקייה הזאת רצים לפעמים כמה סשנים במקביל. אל תעשה `git commit -a` —
   הוא יסחוב איתו עבודה לא גמורה של מישהו אחר. commit רק לקבצים ששינית בשמם.
6. רשימה ריקה + "Unexpected end of JSON input" במסך = ה-API באתחול (pm2 restart),
   לא באג בפיצ'ר.
7. קובץ `.ps1` עם עברית חייב להישמר UTF-8 **עם BOM**.

## מוסכמות UI שנקבעו במסך הזה — שמור עליהן

- **פיצ'ר חדש נכנס ללשונית, לא לבאנר מעל הלשוניות.** צפיפות המסך היא נושא רגיש.
- אייקון לכל סוג עבודה מגיע מ-`client/src/utils/roleIcons.js` — `roleIcon(role, key)`.
  **אל תמפה אייקון לפי התווית בעברית בקוד חדש**; המיפוי הוא לפי מפתח התפקיד
  (`SYSTEM_ROLE_KEYS`), כי אפשר לשנות שם של תפקיד מתוך המערכת. תפקיד חדש → הוסף
  שורה למפה שם, לא במקום השימוש.
- קומפוננטות משותפות שכבר קיימות בקובץ — השתמש בהן במקום לשכפל:
  `PaymentMethodBadge` (תלוש/חשבונית), `RateLine` (שורת תעריף),
  `EmployeeWageAgreementCard`, `MonthlyPayCard`.
- מגירת תיק העובד עובדת עם `drawerTab`; לשונית חדשה בתיק = ערך חדש שם + בלוק
  `{drawerTab === '<x>' && (...)}`, ולא כרטיס נוסף בתחתית הגלילה.
- טקסט למשתמש — עברית. הערות בקוד — עברית, מסבירות **למה** ולא מה.
- דרופדאון הוא `AppSelect`, לא `<select>` רגיל.

## איך לוודא שינוי (חובה — build לבד לא מספיק)

1. `cd client && npx vite build` — חייב לעבור נקי.
2. לרינדור אמיתי: אין דרך להיכנס למסך פנימי בלי התחברות Supabase (`AuthGate`
   חוסם). לכן בונים harness זמני:
   - `client/vite.harness-<feature>.config.js` עם **פורט פנוי** (בדוק
     `netstat -ano | grep ":<port> "` — סשנים אחרים תופסים 3100/3101/5099),
   - `client/harness-<feature>.html`,
   - `client/src/harness-<feature>-main.jsx` שעושה
     `createRoot(...).render(<MemoryRouter><TheScreen /></MemoryRouter>)`,
     מייבא `./index.css`, ומחליף את `window.fetch` בנתוני דמה.
   - **מוחקים את שלושת הקבצים בסוף.**
3. אם חלונית הדפדפן לא מציגה — צילום מסך ייכשל. אז מפעילים את ה-UI דרך
   `javascript_tool` עם `element.click()` ובודקים DOM/`innerText`, לא קואורדינטות.

## מה נשאר לעשות

בסדר עדיפות כפי שהוא נראה כרגע — תאם עם הלקוח לפני שמתחילים בגדול:

1. **חפיפה בין לשוניות** — לשונית "תעודות והסמכות" הראשית, לשונית התעודות בתיק
   העובד, וטור ההסמכות החדש בטבלה מציגים בערך אותו מידע. שווה לאחד או לבטל את
   הראשית.
2. **שעון נוכחות** — כרגע אפשר לרשום כניסה לכל עובד פעיל. הכיוון שהוצע: להציע קודם
   את מי שמשובץ היום בלוח המשמרות.
3. **ייצוא תשלום חודשי** — אין עדיין CSV או דוח להנהלת חשבונות. שים לב לכלל
   "הקפאת שכר": שכר של חודש שכבר נסגר לא זז גם אם התעריפים משתנים.
4. **הקובץ ענק** — `Employees.jsx` מעל 3,600 שורות. פיצול לקבצים (מגירת התיק,
   לשונית התשלום, המודאלים) יקל על כל שינוי הבא. זו עבודה בפני עצמה, לא לעשות
   אותה אגב פיצ'ר.

## מה לא לעשות

- לא לשנות את חישוב השכר (`client/src/utils/wageRates.js` ו-`summarizeWork` /
  `summarizeByRole`) בלי בקשה מפורשת — כל המסך תלוי בו, וגם השרת.
- לא לגעת בלוגיקת `handleClock` ובנתיבי `/api/shifts/*` — הם עובדים.
- לא להוסיף חבילות npm בשביל אייקונים או גרפים; `lucide-react` כבר קיים.
